# Tasks

Atomic, checkable tasks ordered by phase. Each task has:
- An ID (`T-1.3` = Phase 1, task 3).
- References to `requirements.md` and `design.md` sections.
- A definition of "done" — what an agent or reviewer should verify before checking it off.

Mark tasks done by changing `[ ]` to `[x]`. Add a brief note when deviating from the spec.

---

## Phase 0 — Setup
<!-- All Phase 0 setup tasks complete. -->


- [x] **T-0.1 — Bootstrap project from starter**
  - Clone `nuxt-drizzle`, install, run starter as-is to confirm baseline works.
  - Done when: `pnpm dev` serves the starter, `pnpm test:run` passes.
  - **Deviation:** Repo was already bootstrapped past the starter (admin features, item-service, event-bus already present). Rather than re-cloning, verified baseline: `pnpm dev` serves :3000 (HTTP 200), `pnpm typecheck` clean, `pnpm test:run` 39/39 pass.
  - **Fixes carried in:**
    - Buffer global lint error in `server/features/email/templates/index.ts` — added `import { Buffer } from 'node:buffer'`.
    - 3 flaky `item-service.test.ts` filter tests (`_eq`, `_ilike`, `_in`) caused by parallel test files sharing the dev DB while `afterEach` truncates. Fixed by setting `fileParallelism: false` in `vitest.config.ts`.
  - **Follow-up:** A dedicated test DB (or per-worker schema) is needed before integration test volume grows. Tracked as new task **T-0.0a** below; address before tests start to feel slow.

- [x] **T-0.2 — Enable `pgvector` extension**
  - Add migration: `CREATE EXTENSION IF NOT EXISTS vector;`
  - Refs: REQ-COMMON-4.
  - Done when: Migration applies cleanly; no schema uses vector columns yet.

- [x] **T-0.3 — Add new env vars**
  - Add Orchestrator and GitHub vars to `.env.example` and `nuxt.config.ts` runtimeConfig.
  - Refs: DESIGN-ENV.
  - Done when: New vars are typed and accessible via `useRuntimeConfig()`.
  - **Deviation:** Codebase has no `runtimeConfig` block — all existing env vars (DB, auth, email, storage) are read via `server/utils/config.ts` from `process.env` into a typed `config` object. New vars (`NUXT_ORCHESTRATOR_HISTORY_LIMIT`, `NUXT_GITHUB_SYNC_INTERVAL_MINUTES`, `NUXT_GITHUB_COMMITS_PER_SYNC`) follow the same pattern (added to `.env.example` and to `config.orchestrator` / `config.github` in `server/utils/config.ts`). See **ADR-016** for the rationale.
  - **Out of scope here:** `NUXT_PUBLIC_I18N_DEFAULT_LOCALE` from DESIGN-ENV is deferred to T-0.5, which is the i18n install task.

- [x] **T-0.4 — Add new permissions and seed**
  - Extend `server/features/rbac/permissions.ts` with the 13 new permissions (incl. `ai:read`, `ai:manage`).
  - Update seed to assign per DESIGN-RBAC table.
  - Refs: REQ-COMMON-1, DESIGN-RBAC.
  - Done when: Fresh DB seed produces roles with the new permissions; existing tests still pass.
  - **Deviation:** The original `seedRbac` skipped existing roles entirely (no permission reconciliation). To make incremental upgrades possible without a full DB wipe, the seed now also adds any missing permissions to already-seeded system roles (it never removes). Behaviour for fresh DBs is unchanged.

- [x] **T-0.5 — Install and configure `@nuxtjs/i18n`**
  - Add module to `nuxt.config.ts` with locales `de` (default) and `en`.
  - Create `i18n/locales/de.json` and `i18n/locales/en.json` with seed strings for the auth and shell pages already present in the starter (so existing pages don't regress).
  - Add language switcher component to the top nav.
  - Persist user's language preference on the user/profile (extend the starter's profile feature with a `locale` field).
  - Add `crypto_salt` column to `organisations` (for DESIGN-CRYPTO).
  - Refs: REQ-I18N-1, REQ-I18N-2, DESIGN-I18N, DESIGN-CRYPTO.
  - Done when: Switching language updates UI strings live; preference persists across sessions; existing tests pass.
  - **Deviations:**
    - `@nuxtjs/i18n` v10 dropped the top-level `lazy` option (lazy loading is the default now); it was omitted from the config rather than passed explicitly. `langDir` defaults to `'i18n/locales'` in v10, so the locale files live there with no explicit `langDir` setting.
    - `useI18n()` cannot be called from a Nuxt plugin (vue-i18n composable is component-only). The boot-time locale-sync plugin (`app/plugins/i18n-locale.ts`) reaches the global instance via `nuxtApp.$i18n.setLocale` instead.
    - Migration `0002_steep_blonde_phantom.sql` was hand-edited from the autogenerated SQL: it adds `organisations.crypto_salt` as nullable, backfills existing rows with a random 32-char hex via `md5(random()::text || clock_timestamp()::text)` (avoids needing the `pgcrypto` extension), then sets `NOT NULL`. New rows get a real `crypto.randomBytes(16)` salt from `organisationsService.create`.
    - `tests/factories.ts` `createOrganisationData` now includes `cryptoSalt` so tests that bypass the service layer (item-service, event-bus) keep inserting valid rows.
    - Anonymous users' locale is persisted in a cookie (`i18n_locale`); authenticated users persist it on `users.locale` via `PUT /api/profile`.
  - **Process reminder (per ADR-012):** any future task that introduces user-facing strings must update both `i18n/locales/de.json` and `i18n/locales/en.json` in the same change. Strings outside the auth pages and immediate shell are intentionally left as-is for this task and should be retro-fitted by the owning task.

- [x] **T-0.6 — Shared crypto utility**
  - Implement `server/utils/crypto.ts` with `encryptForOrg` / `decryptForOrg` per DESIGN-CRYPTO.
  - Unit tests cover round-trip, tamper detection, salt isolation between orgs.
  - Refs: DESIGN-CRYPTO.
  - Done when: Tests pass; module is ready to be consumed by GitHub and AI features.
  - **Deviation:** the implemented signatures take the org's `crypto_salt` directly (`encryptForOrg(plaintext, orgSalt)` / `decryptForOrg(blob, orgSalt)`) rather than the `orgId`-based shape sketched in DESIGN-CRYPTO. The salt is the only piece of the organisation that actually feeds key derivation, and decoupling the utility from the database keeps it pure (no DI / no DB lookup). Consumers (GitHub, AI) will load `organisations.cryptoSalt` once and pass it in. Blob format is `base64(iv):base64(tag):base64(ciphertext)`, matching the design's "`<iv>:<ciphertext>:<tag>` base64-joined" with the tag and ciphertext order swapped so decryption can validate IV/tag length before allocating the ciphertext buffer.

- [x] **T-0.7 — `/app` layout and shell**
  - Create `app/layouts/app.vue` with the hub's top nav (workspace switcher, global search placeholder, language toggle, orchestrator launcher placeholder) and side nav (KB / Todos / Projects / Orchestrator / Settings — entries can be stubs that link to not-yet-existing pages).
  - Create `app/pages/app/index.vue` as a minimal hub landing page (welcome / quick stats placeholder).
  - All `/app/**` pages going forward must use the `app` layout.
  - Confirm `/admin/**` and `/` remain untouched.
  - Refs: DESIGN-ROUTES, DESIGN-FRONTEND, ADR-015.
  - Done when: Visiting `/app` shows the new layout; `/admin` still shows the starter admin layout; `/` is unchanged.
  - **Deviation:** Workspace switcher renders a static placeholder label (`app.shell.workspaceSwitcher.placeholder`) instead of the active organisation name — the session payload (`AuthUser`/`SessionResponse`) does not currently expose an active organisation, and adding that wiring is out of scope for the layout shell. Replacing the label is a TODO marked in the layout source.

---

## Phase 1 — Knowledge Base

### Schema
- [x] **T-1.1 — Drizzle schema for KB tables**
  - Tables: `kb_entries`, `kb_categories`, `kb_tags`, `kb_entry_tags`, `kb_entry_links`.
  - Add tsvector generated column on `kb_entries.body_search` and a GIN index.
  - Refs: DESIGN-DATA §KB.
  - Done when: `pnpm db:push` succeeds; tables visible in Drizzle Studio with expected indexes.

### Server feature: kb
- [x] **T-1.2 — KB service skeleton**
  - `server/features/kb/service.ts` with CRUD on entries, categories, tags. Use `ItemService` where it fits; custom methods for slug generation and search.
  - Refs: REQ-KB-1, REQ-KB-2, REQ-KB-3.
  - Done when: Unit tests cover create, update, find by slug, list with filters.
  - **Deviation:** Slug-collision suffixing is intentionally deferred to T-1.3 per the task spec. `kbEntryService.create` calls a minimal `slugify` (lowercase + non-alphanum-to-`-`, no umlaut handling) and on `(organisation_id, slug)` unique violation throws `KbSlugConflictError`. T-1.3 will replace this branch with transparent `-2`/`-3`/... suffixing; until then duplicate-title creates raise the error rather than auto-suffixing. Both behaviours are covered by tests.

- [x] **T-1.3 — Slug generation**
  - `slugify(title)` plus collision suffix per workspace.
  - Refs: REQ-KB-1.
  - Done when: Tests cover umlauts, punctuation, collision suffixing.

- [x] **T-1.4 — Markdown + wikilink parsing**
  - `server/features/kb/markdown.ts`: extract `[[slug]]` and `[[Title|slug]]` patterns; expose `parseWikilinks(body)`.
  - On entry save, transactionally rebuild `kb_entry_links` for that entry.
  - Refs: REQ-KB-4, DESIGN-WIKILINKS.
  - Done when: Tests cover both syntaxes, escaping, code-block exclusion (links inside ``` blocks must not match).
  - **Deviation:** Indented (4-space) code blocks are NOT treated as code by the parser — only fenced (``` and ~~~) and inline (`` `...` ``) code is excluded. CommonMark would also exclude 4-space-indented blocks; we scoped to fenced + inline per the task brief because indented blocks are rare in practice and adding paragraph-context tracking to a regex pre-pass adds disproportionate complexity. If this becomes a real problem we'd swap the masker for a CommonMark AST walker.
  - **Notes:**
    - Slug normalisation: the parser returns the literal slug as written (whitespace-trimmed). DESIGN-WIKILINKS says the resolution slug is "lowercased and slugified the same way as title-derived slugs"; the service-side resolver currently performs an exact-string lookup against `kb_entries.slug` (no extra slugifying step). All test fixtures use already-canonical lowercase slugs, matching how the editor will emit them in T-1.9. If a future requirement demands `[[Foo Bar]]` to resolve against slug `foo-bar`, that's a one-line `slugify(targetSlug)` in `rebuildEntryLinks` — out of scope for this task.
    - Dedup happens at the service layer (one `kb_entry_links` row per unique `to_slug` per source entry, per DESIGN-WIKILINKS step 2). The parser deliberately preserves duplicates so renderers can keep their occurrence count.

- [x] **T-1.5 — Full-text search**
  - Generated tsvector column with weighted title (A) + body (B). Query via `to_tsquery` with prefix matching.
  - Refs: REQ-KB-5, DESIGN-DATA, DESIGN-RANK.
  - Done when: Search test fixture returns entries ranked by title match before body match.

- [x] **T-1.6 — Status lifecycle and inbox/trash views**
  - Service methods `setStatus`, `softDelete`, `restore`. Default scope filters `deleted_at IS NULL` and excludes `archived` unless asked.
  - Refs: REQ-KB-7, REQ-KB-8, REQ-KB-9.
  - Done when: Tests cover all status transitions and the default exclusion of `archived`/`deleted`.
  - **Deviation:** the task brief suggested a constrained transition matrix (e.g. `verified` cannot go back to `inbox`). REQ-KB-7 explicitly states "THE SYSTEM SHALL allow the user to transition any entry between any of the four statuses", so the implementation honours the looser rule: `isValidStatusTransition` accepts every ordered pair of valid enum values, including same-status no-ops. The validation/error infrastructure (`KbInvalidStatusTransitionError`, shared between `setStatus` and `update({ status })`) is in place so a future tightening is one function edit. The error fires today only for unknown enum values (defence in depth at the service layer).

### API
- [x] **T-1.7 — KB API routes**
  - All endpoints in DESIGN-API §KB plus the additions called out by the task brief (status + purge).
  - Permission guards: `kb:read` for GET, `kb:write` for POST/PATCH and the restore route, `kb:delete` for destructive endpoints.
  - Zod schemas (v4) co-located in `server/features/kb/schemas.ts`; enum tuples re-exported from `server/features/kb/types.ts` so schema and DB stay in sync.
  - Workspace context: resolved in `server/features/kb/workspace.ts` from `X-Organisation-Id` header (or `?organisationId=` query) with membership validation; falls back to the user's earliest-joined organisation. Temporary fallback until the workspace switcher persists the active workspace on the session.
  - Refs: REQ-KB-1..9, DESIGN-API, ADR-001, ADR-009, ADR-012.
  - Done when: Manual smoke test via REST client passes for create/list/get/update/soft-delete/restore/backlinks/tags.
  - **Deviations:**
    - Added two routes not in DESIGN-API §KB but explicitly required by the task brief: `POST /api/kb/entries/[id]/status` and `DELETE /api/kb/entries/[id]/purge`. Both map to existing service methods (`setStatus`, `purge`); no service surface added.
    - The `GET /api/kb/entries/[id]` route also accepts a slug as a transparent fallback so the upcoming UI can route on either token without an extra round-trip. Pure additive convenience; id resolution still wins.
    - Route-level integration tests deferred — the existing `tests/` harness drives services directly and stands in for service-level coverage. A Nuxt-test-utils harness for full-stack route auth/permission cases is a follow-up (track with T-1.8/T-1.9 since they will need the same harness for page-level smoke tests). Service coverage remains green at 154/154.

### Client feature: kb
- [x] **T-1.8 — KB list and detail pages**
  - `app/pages/app/kb/index.vue`, `app/pages/app/kb/[slug].vue`.
  - Search box with FTS, filter chips (tag, category, status, author type, source type).
  - Pinia Colada queries.
  - Refs: REQ-KB-5, DESIGN-FRONTEND.
  - Done when: Pages render lists, navigate to detail, filters change query results.
  - **Deviations:**
    - The list view renders entries as a card list rather than `UDataTable`. At this density (variable-length tag/category metadata, optional AI-author indicator) cards read better than a multi-column table; the admin blog-posts page uses the same shape.
    - Tag filter is a single-select today, not a multi-select. The server's `listKbEntries` filters by a single `tagId`; multi-tag filtering is a TODO once the API supports it.
    - The `GET /api/kb/entries` route does not return a total count, so the list page uses prev/next buttons (with "is current page full?" as the has-next heuristic) rather than `UPagination`. Switching to `UPagination` is a one-line change once the API exposes a total.
    - Markdown body on the detail page renders as `<div class="whitespace-pre-wrap font-mono">`. T-1.9 introduces the editor + preview pair and will swap in the proper renderer with wikilink resolution; using a Markdown lib here would have to be replaced again, so the placeholder is intentional.
    - The "Create entry" button links to `/app/kb/new`, which T-1.9 will add. Clicking it today 404s — accepted per the task brief.
    - Detail page "Edit" links to `/app/kb/[slug]/edit` (T-1.9); "Delete" is a disabled stub (T-1.10 owns the delete action from the trash workflow).
    - Workspace context: no `X-Organisation-Id` header is sent. The session payload still doesn't expose the active workspace, so the server's earliest-org fallback applies — same behaviour as T-1.7 manual smoke tests. The API wrapper (`app/features/kb/api/kb.api.ts`) is the right place to inject the header once the workspace switcher persists a selection.

- [x] **T-1.9 — Markdown editor with preview**
  - Split editor + preview. Wikilinks render as `<a>` to the target slug, with a visual indicator for unresolved links.
  - Tag and category pickers integrated.
  - Refs: REQ-KB-1, REQ-KB-4, DESIGN-FRONTEND.
  - Done when: Creating and editing an entry round-trips Markdown faithfully; wikilinks in preview navigate.
  - **Deviations:**
    - Editor stack: a plain `<textarea>` (left) plus rendered preview pane (right) instead of Nuxt UI's TipTap-backed `UEditor`. TipTap's `content-type="markdown"` mode does not round-trip Markdown losslessly (footnotes, raw HTML, advanced wikilink-style tokens, code-block whitespace). For a wiki where the body is authoritative Markdown and `[[wikilinks]]` are first-class, a textarea + live preview is simpler and faithful. Tag/category pickers reuse Nuxt UI's `USelectMenu` with `create-item`, matching the admin blog form pattern.
    - Markdown renderer: `marked@17` (already in the dep tree via `@tiptap/markdown`). Imported directly — relying on the transitive package keeps the install footprint flat and `shamefully-hoist=true` makes the resolution legitimate. Preview pipeline: parse wikilinks (shared parser), swap each `[[...]]` for a Unicode-PUA marker, run marked, then post-process markers into `<a class="kb-wikilink ...">` tags. The PUA marker can't collide with user content and is invisible to marked's tokeniser. Wikilinks inside fenced or inline code pass through verbatim because the shared parser already excludes those regions.
    - Wikilink resolution: added `GET /api/kb/resolve-slugs?slugs=a,b,c` returning `{ resolved: [...] }`. Permission `kb:read`, Zod-validated array (max 100 slugs, 200 chars each). The route lives at `/api/kb/resolve-slugs` rather than the more obvious `/api/kb/entries/by-slugs` because the latter would overlap the parametric `[id]` route in the typed Nitro route map and narrow templated `${id}` `$fetch` calls to the intersection of methods (GET only, breaking PATCH/DELETE typings on the entry routes). Client composable `useKbResolveSlugs` keys on the sorted unique slug list; an empty list short-circuits with no network call.
    - SPA navigation in preview: the renderer emits real `<a href>` tags so server-rendered HTML is link-correct without JS. The preview component intercepts left-click without modifier keys and routes via `useRouter().push()`; modifier-key clicks (cmd/ctrl/shift/alt) and middle-click fall through so "open in new tab" still works.
    - Wikilink parser was extracted from `server/features/kb/markdown.ts` to `shared/kb/markdown.ts` so the client renderer reuses the exact same parser. The server file is now a one-line re-export. The existing test (`tests/kb-markdown.test.ts`) still imports from `server/features/kb/markdown` and stays green. Tests use a relative-path import for the shared module because the vitest `~~` alias resolves to the project root with a trailing-slash quirk.
    - Markdown features intentionally not rendered: mermaid, KaTeX, advanced GFM extensions beyond `marked`'s defaults. `marked` ships with GFM tables / autolinks / strikethrough on by default; everything else is plain CommonMark. Image rendering follows `marked` defaults (no media-picker integration) — the upload-from-editor flow is admin-blog-only, not KB.
    - Unresolved-link styling: dotted underline + a small `?` badge. Resolved links get a solid border; both navigate to `/app/kb/<slug>` so creating a missing entry from the preview is one click.
    - Detail page (`app/pages/app/kb/[slug].vue`) now renders the body via the same `KbMarkdownPreview` component instead of the `whitespace-pre-wrap` placeholder shipped in T-1.8.
    - Form is shared between create and edit (`app/features/kb/components/KbEntryForm.vue`) so both pages stay thin. Default status is `draft` for human authors per REQ-KB-1; AI-author defaults are handled server-side and out of scope here.
    - New tests: `tests/kb-render-markdown.test.ts` covers the renderer's resolved/unresolved classes, the `[[Title|slug]]` form, code-block exclusion (fenced + inline), plain-Markdown pass-through, and `extractWikilinkTargets` ordering. Suite count moves from 154 → 164.

- [x] **T-1.10 — Inbox and Trash views**
  - `app/pages/app/kb/inbox.vue` and `app/pages/app/kb/trash.vue` with one-click actions.
  - Refs: REQ-KB-8, REQ-KB-9.
  - Done when: Inbox lists `status='inbox'` entries with Verify / Draft / Archive / Delete buttons; Trash lists `deleted_at IS NOT NULL` with Restore / Purge.
  - **Deviations / notes:**
    - Sub-navigation: implemented as a horizontal **tab strip** (`KbSubNav.vue`) rendered inside each KB top-level page (`/app/kb`, `/app/kb/inbox`, `/app/kb/trash`), not as nested side-nav entries. Lower-friction for inbox-triage UX and keeps the global side nav scoped to feature-level entries.
    - Action buttons render **inline per row** (Verify/Draft/Archive/Delete on inbox cards; Restore/Purge on trash cards). No dropdown menu — bulk actions are out of scope (T-NTH-2).
    - Confirm modals: a single-step `UModal` for both Soft-Delete (inbox) and Purge (trash). Type-to-confirm was deemed overkill for a single-user hub; the body explicitly states irreversibility and the CTA is `color="error"`. Body translation (DE: *„endgültig gelöscht … kann nicht rückgängig gemacht werden"*).
    - `useKbEntryMutations` extended with `useVerifyKbEntry` / `useMarkKbEntryDraft` / `useArchiveKbEntry` (thin `setStatus` wrappers) and `usePurgeKbEntry` (DELETE → 204). Centralised invalidation now also wipes `inbox` + `trash` query families so mutations on either page keep both lists fresh.
    - Pagination matches the list page (prev/next + "is page full?" heuristic) since the API still returns no total.
    - No new tests added — pages exercise existing service + API paths; suite stays at 164/164.

- [x] **T-1.11 — Categories tree UI**
  - Sidebar with a collapsible tree; selecting a category filters the entry list.
  - Refs: REQ-KB-3.
  - Done when: Nested categories render correctly; clicking shows entries from selected and descendants.
  - **Deviations / notes:**
    - Descendant filter implemented **server-side** via a recursive CTE on `kb_categories.parent_id` in `kbEntryService.list` (REQ-KB-3 "list entries by category, including descendants"). Gated behind a new opt-in flag `includeDescendantCategories` so existing single-category callers keep their literal-match behaviour. The CTE restricts to the same workspace + `deleted_at IS NULL` so trashed sub-trees don't leak. New Zod field on the list-entries query schema (also opt-in). The `/app/kb` UI passes the flag whenever a category is selected — the dropdown and the tree therefore behave identically.
    - Tree component: `app/features/kb/components/KbCategoryTree.vue`. Builds a nested tree client-side via reduce, sorts siblings alphabetically with a locale-aware `Intl.Collator`, walks to a flat `{ category, depth, hasChildren, expanded }` array so the template stays linear (no recursive component). Lucide chevron-right/down toggles. "All entries" pseudo-row clears `categoryId` (sets the URL query to empty).
    - Expand-state persistence: `useLocalStorage<string[]>('kb:cat-tree:expanded', [])` from `@vueuse/core` (auto-imported via `@vueuse/nuxt`), wrapped through a `Set<categoryId>` computed for O(1) lookups. Survives reloads.
    - Inline CRUD scope: shipped — "+" header for top-level create, per-row `UDropdownMenu` (visible on hover/focus) with **Add child / Rename / Delete**. Modals reuse the existing inbox/trash UModal+UCard pattern. Delete is soft (matches the existing `DELETE /api/kb/categories/[id]` semantics, ADR-009). No bulk operations (per task hard limits).
    - Mutation composables added to the existing `useKbEntryMutations.ts` (rather than a separate file) because `useCreateKbCategory` already lived there and a separate file caused an auto-import collision: `useRenameKbCategory`, `useDeleteKbCategory`. All three category mutations now invalidate both `kbKeys.categories()` and `kbKeys.entries()` so a rename refreshes the list view's hydrated category badge without manual plumbing.
    - Responsive layout: at `lg` and above, `/app/kb` uses a `lg:grid lg:grid-cols-[240px_minmax(0,1fr)] lg:gap-6` split — tree on the left rail, search/filters/results on the right. Below `lg` the tree is hidden (`hidden lg:block`) and the existing single-select category dropdown is shown (`min-w-40 lg:hidden`). One affordance per breakpoint, no duplication.
    - Entry counts per category: not implemented — would have required either an N+1 client fan-out or a dedicated server aggregation endpoint, both bigger than this slot. Flagged as a follow-up.
    - New service tests (`tests/kb-service.test.ts`): three covering (a) opt-in only — single-category filter still matches one category by default, (b) immediate children expansion, (c) multi-level descendants including the anchor category. Suite count moves from 164 → 167.
    - Quality gates: `pnpm lint`, `pnpm typecheck`, `pnpm test:run` 167/167 green. Smoke: `curl /app/kb` returns 302 → `/auth/sign-in` (expected, no 500).

### Phase 1 acceptance
- [x] **T-1.X — Phase 1 complete**
  - End-to-end: create category → create entry with tags and a wikilink → search finds it → backlinks appear on the linked entry → status flow works → soft delete and restore work.
  - Done when: A manual run-through of the above succeeds in two different workspaces with no data leak.
  - **Deviation / fix:** the seed admin user (`admin@example.com`) was given the `Super Admin` global role but never an organisation membership, so every workspace-scoped API (`/api/kb/*`) returned `workspace.no_membership` from the earliest-membership fallback. Resolved by extending `seedAdmin` in `server/plugins/01.init.ts` to idempotently create a `Default Workspace` (slug `default`) and add the admin as Owner whenever the admin has no memberships yet (~15 lines, reuses `container.organisationsService.create`). T-1.7's earliest-membership fallback now resolves on the very first run.
  - **Manual run transcript (workspace 1, ID `01KQTT63C9Q01RT9G2SJHBEX4C`):**
    - `POST /api/auth/login` → 200 (admin signed in).
    - `GET /api/kb/categories` → 200 (was 403 `workspace.no_membership` before the seed fix).
    - `POST /api/kb/categories {name:'Bücher'}` → 201 (slug `buecher`).
    - `POST /api/kb/entries` (title `Vue Notizen`, body with `[[react-notizen]]` + `[[Cooking|cooking]]`, tags `vue`/`frontend`, category Bücher) → 201, status `draft`.
    - `POST /api/kb/entries` (title `React Notizen`) → 201, slug auto-`react-notizen`.
    - `GET /api/kb/entries?search=Notizen` → 200, both entries returned (title-rank A on each).
    - `GET /api/kb/entries/<react-id>/backlinks` → 200, returns `[{slug:'vue-notizen', title:'Vue Notizen'}]`.
    - `POST /api/kb/entries/<vue-id>/status {verified}` → 200; `{archived}` → 200; `{draft}` → 200.
    - `DELETE /api/kb/entries/<vue-id>` → 200 (soft-delete, `deletedAt` set).
    - `GET /api/kb/trash` → 200, includes Vue Notizen with full tag + category metadata.
    - `GET /api/kb/entries` → 200, only React Notizen returned (Vue Notizen correctly hidden).
    - `POST /api/kb/entries/<vue-id>/restore` → 200, `deletedAt` cleared.
    - `GET /api/kb/entries/vue-notizen` → 200, slug-fallback resolves the restored entry with tags + category.
  - **Workspace 2 (`01KQTTENNCPZTCFHN00B9BD2M7`, slug `second`):**
    - `POST /api/organisations {name:'Second Workspace',slug:'second'}` → 200.
    - `POST /api/kb/categories` with `X-Organisation-Id: <ws2>` → 201.
    - `POST /api/kb/entries` (title `Vue Notizen` again) with `X-Organisation-Id: <ws2>` → 201, slug `vue-notizen` (no collision — `(organisation_id, slug)` unique).
    - `GET /api/kb/entries` with `X-Organisation-Id: <ws2>` → 200, returns only the WS2 entry; WS1 data not visible. Cross-workspace isolation holds.
  - **Gates:** `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 167/167.

---

## Phase 2 — Todos

### Schema
- [x] **T-2.1 — Drizzle schema for todos**
  - Tables: `todos`, `todo_tags`, `todo_kb_links`. Reuse `kb_tags`.
  - Refs: DESIGN-DATA §Todo.
  - Done when: `pnpm db:push` applies cleanly.
  - Done: migration `0004_condemned_puck.sql` creates `todo_priority` enum + 3 tables; FKs cascade on parent/org/todo/tag/entry deletion; `created_by` set null on user delete; indexes `(org, completed_at)`, `(org, due_at)`, `(parent_todo_id)` per spec; junctions use composite PKs. No deviations from DESIGN-DATA §Todo. Subtask depth-3 invariant (REQ-TODO-2) is enforced in service (T-2.2), not the DB — documented in `todos.ts`.

### Server feature: todos
- [x] **T-2.2 — Todos service**
  - CRUD, complete/uncomplete, link/unlink KB, tag set replace.
  - Subtask depth check (max 3) enforced in service.
  - Refs: REQ-TODO-1, REQ-TODO-2, REQ-TODO-3.
  - Done when: Unit tests cover all of the above and reject depth > 3.
  - **Deviations / notes:**
    - Subtask soft-delete is **not** cascading (children may still be relevant after the parent is trashed). The default `list` scope hides only the soft-deleted row itself; siblings/descendants stay visible. Hard `purge` does cascade via the schema's `parent_todo_id ON DELETE CASCADE`.
    - Search uses Postgres `ILIKE` on `title` + `description_md` (no FTS for todos in DESIGN-DATA). Whitespace-only `search` is treated as no filter.
    - Subtask-depth check is a single recursive CTE walking ancestors from the proposed parent — one round-trip, workspace-scoped (a parent in another org returns no rows and surfaces as `TodoParentNotFoundError`). `update({ parentTodoId: <self> })` is rejected up-front to avoid a free-running CTE on a self-cycle.
    - Tag rebuild reuses `kbTagService.findOrCreate` (ADR-008 — todos and KB share the `kb_tags` table). Tag rows are resolved outside the create/update transaction (idempotent, no lock contention); the junction rewrite happens inside the same transaction as the row insert/update.
    - KB-link rebuild validates every supplied entry id is in the same workspace (`TodoKbLinkNotFoundError` on miss) before opening the transaction.
    - `linkKb` / `unlinkKb` / `linkTag` / `unlinkTag` standalone helpers are idempotent (insert-if-absent, delete-if-present). Tag-set / link-set replacement is via `update({ tagNames })` / `update({ kbEntryIds })` — same as KB.
    - New tests: 32 in `tests/todo-service.test.ts` (CRUD, depth 1–3 + depth-4 rejection, re-parenting depth check, cross-workspace parent rejection, tag-set replace + cross-ws isolation, KB-link replace + cross-ws rejection, idempotent link helpers, complete/uncomplete idempotency, list filters incl. completed/priority/dueBefore/parent/search/tag/kb, purge with cascade). Suite: 167 → 199 green.

- [x] **T-2.3 — Todo views (today/upcoming/open/completed)**
  - Service methods returning the four canonical views, plus filters by tag and priority.
  - Refs: REQ-TODO-4.
  - Done when: Tests verify view logic with fixture data covering edge cases (overdue, due today, due tomorrow).
  - **Implementation:** `listToday` / `listUpcoming` / `listOpen` / `listCompleted` on `todoService`, plus `getViewCounts` for tab badges. All views hard-code their date / completion predicate and reuse a shared `buildBaseConditions` helper for tag / priority / kbEntryId / search / parent filters (so view + filter compose identically to `list`). Date math runs server-side against `current_date` (UTC) via `due_at::date <= current_date` style predicates — no JS-side bounded dates passed in, so request-vs-query drift is impossible.
  - **Deviations:**
    - **Timezone:** all four views use UTC for "today" / "+N days". REQ-TODO-4 is silent on timezone handling and the rest of the system stores UTC throughout — per-user-timezone is deferred to a later task.
    - **`listCompleted` returns the full set,** not "last 30 days" as REQ-TODO-4 suggests. The 30-day window is a UI concern (T-2.5); keeping the service-side surface unfiltered means the UI can show a longer history if it wants to without a service change. The sort is `completed_at DESC` so naturally newer rows surface first.
    - **`getViewCounts` is implemented** as 4 parallel queries (Promise.all) rather than a single CTE — the queries are cheap thanks to existing indexes and the simpler shape is easier to keep consistent with each `list*` view's predicate.
  - **Tests:** added `describe('todoService — canonical views (REQ-TODO-4)')` block in `tests/todo-service.test.ts` with 12 cases covering: today contents, today sort (due ASC + priority tiebreak), upcoming default 7d, upcoming `withinDays: 14`, open contents, open sort (priority DESC + due NULLS LAST), completed contents/sort, priority filter compose × 4 views, tagId filter compose × 4 views, workspace isolation × 4 views, soft-deleted excluded × 4 views, getViewCounts parity. Suite: 199 → 211 green.

### API
- [x] **T-2.4 — Todo API routes**
  - All endpoints in DESIGN-API §Todos.
  - Permission guards: `todo:read`, `todo:write`, `todo:delete`.
  - Refs: DESIGN-API.
  - **Implementation:** 17 route files under `server/api/todos/**` (CRUD: index.get/post, [id].get/patch/delete; transitions: complete.post / uncomplete.post / restore.post / purge.delete; links: kb-links.post + kb-links/[entryId].delete + tags.post + tags/[tagId].delete; views: today / upcoming / open / completed / counts). Zod v4 schemas at `server/features/todo/schemas.ts`, error-mapping helpers at `server/features/todo/api-utils.ts` (mirrors KB pattern). Workspace context resolves via the shared `resolveWorkspace` from `server/features/kb/workspace.ts` (`X-Organisation-Id` header → `?organisationId` → first membership). Permissions are scoped to the resolved organisation: `todo:read` for reads, `todo:write` for writes (incl. complete/uncomplete/restore/link/unlink), `todo:delete` for soft-delete and purge. Service errors are mapped to stable codes (`todo.not_found`/404, `todo.subtask.depth_exceeded`/409, `todo.parent.not_found`/404, `todo.kb_link.not_found`/404, `todo.purge.active`/409, `validation.failed`/400).
  - **Deviations from DESIGN-API §Todos:**
    - **`POST /api/todos/[id]/kb-links`** semantics: DESIGN-API describes "Replace KB link set"; the implemented route adds a single link (`{ entryId }`) per the T-2.4 task brief. Bulk replacement is available via `PATCH /api/todos/[id]` with `{ kbEntryIds: [...] }`.
    - **`POST /api/todos/[id]/tags`** uses replacement semantics with `{ tagNames: string[] }`, matching the convention used by `POST /api/kb/entries/[id]/tags` (ADR-008 — todos and KB share `kb_tags`). Names are findOrCreate'd by the service.
    - **Added (not in DESIGN-API §Todos):** `DELETE /api/todos/[id]/kb-links/[entryId]`, `DELETE /api/todos/[id]/tags/[tagId]`, `POST /api/todos/[id]/restore`, `DELETE /api/todos/[id]/purge` (idempotent granular unlinks + Trash flow). All required by the T-2.4 brief and symmetric with the KB API surface (T-1.7).
    - **Added view convenience:** `GET /api/todos/today`, `/upcoming`, `/open`, `/completed`, and `/counts` (all four canonical views as discrete endpoints + a counts aggregate, per the T-2.4 brief; DESIGN-API folds them into `GET /api/todos?view=…`). The discrete routes wrap `todoService.list{Today,Upcoming,Open,Completed}` and `getViewCounts` directly.
  - **Smoke test (admin@example.com / Default Workspace):** `POST /api/todos`→201, `GET /api/todos`→200, `GET /api/todos/[id]`→200, `PATCH /api/todos/[id]`→200, `POST .../complete`→200, `POST .../uncomplete`→200, `DELETE /api/todos/[id]`→200, list default omits soft-deleted (✓), `?includeDeleted=1` includes (✓), `POST .../restore`→200, `today/upcoming/open/completed/counts`→200, `POST .../tags`→200 (tag findOrCreate), `DELETE .../tags/[id]`→204, `POST .../kb-links`→201, `DELETE .../kb-links/[id]`→204, `DELETE .../purge` on active→409 (`todo.purge.active`), soft-delete + purge→204, GET after purge→404, validation `priority:"bogus"`→400, link nonexistent KB id→404 (`todo.kb_link.not_found`), unauthenticated POST→401.

### Client feature: todos
- [x] **T-2.5 — Todo list pages**
  - `app/pages/app/todos/index.vue` with the canonical views as tabs/segments.
  - Refs: REQ-TODO-4.
  - Done when: Switching views changes the rendered list correctly.
  - **Implementation:** vertical-slice scaffold under `app/features/todo/` (api wrappers + Pinia-Colada keys, four view composables — `useTodos`, `useTodoView`, `useTodoCounts`, `useTodoMutations`, plus a `TodoSubNav` component). The page itself is a single shell with a `?view=...` URL param swap rather than four separate routes — switching tabs swaps the active view and the underlying Pinia-Colada query while keeping search/filter/page state on the URL. Each tab shows a small count badge sourced from `GET /api/todos/counts`. Per-row UX: completion checkbox on the left (toggles complete/uncomplete with an optimistic toast), title + priority chip (urgent → red, high → orange, medium → grey, low → blue) + relative due date in red when overdue and active, and a kebab menu with Edit (links to `/app/todos/[id]/edit` — T-2.6 territory) and Delete (soft delete with toast). Mutations funnel through a shared `invalidateTodo` helper that busts the entire `todos` query family so all four views and the counts badge stay coherent after a state transition.
  - **Deviations from the brief:**
    - **Tag filter is single-select** (not multi). Server `tagId` is a single id today; a multi-select bar would imply a client-side OR which is misleading. Defer until the API accepts `tagIds[]`.
    - **No row-level confirmation modal for soft-delete.** Toast feedback on success/error; restore is one click from `/app/todos?view=...` once T-2.7/T-2.8 surface trash. Two-step confirmation is appropriate for hard-delete (KB pattern); soft-delete here is reversible.
    - **`subtaskCount` and KB-link counts are not surfaced on rows.** The list endpoints return bare rows without hydrated tags / subtask counts / link counts; rather than do an N+1 fetch from the page, the visual indicators are deferred to T-2.6 (which can introduce a hydrated list response) and T-2.7 (subtask UI specifically).
    - **`topLevel: true` is forced on every view.** Subtasks render under their parents in T-2.7, so flattening them into the four canonical views would double-count. The server already supports this filter.
    - **No tests added** — UI shell over four already-tested view endpoints. Suite remains at 211/211.
  - **Smoke test:** `curl /app/todos` unauthenticated → 302 to `/auth/sign-in?redirect=/app/todos` (no 500). Authenticated curl → 200 with `<title>Aufgaben - …</title>` (German locale via admin's saved preference) and i18n-bound tab labels in the SSR HTML.

- [x] **T-2.6 — Todo detail / edit**
  - Side-panel or page for editing a todo: title, Markdown description, priority, due date, parent, tags, KB links picker.
  - Refs: REQ-TODO-1..3.
  - Done when: All fields editable; KB picker autocompletes against KB entries in the workspace.
  - **Implementation:** shared `TodoForm.vue` reused by `app/pages/app/todos/new.vue` and `app/pages/app/todos/[id]/edit.vue` (mirrors the KB pattern from T-1.9). A small read-only detail page at `app/pages/app/todos/[id]/index.vue` is the landing point for the create/edit redirect — it renders the description (via the same `renderKbMarkdown` helper as KB so wikilinks behave consistently), tags, linked KB entries (clickable chips into `/app/kb/<slug>`), an immediate subtask count placeholder (full subtask UI is T-2.7), and Edit / Complete buttons. Mutations: `useCreateTodo` + `useUpdateTodo` added to `useTodoMutations.ts`, both funnel through the existing `invalidateTodo` helper so all four canonical view caches, the counts badge, and the by-id cache stay coherent. `useTodoById` composable hydrates the form against `GET /api/todos/[id]`.
  - **Pickers:**
    - Parent — `TodoParentPicker.vue` is a `UInputMenu` typeahead bound to `GET /api/todos?search=...&limit=10` via `v-model:search-term`. Self-selection filtered client-side; descendant rejection lives server-side and surfaces as a toast (the heuristic in the form maps `depth` errors to a friendly i18n string).
    - Tags — reuses `KbTagPicker.vue` directly (ADR-008: tags shared per `kb_tags`). Names are submitted as `tagNames`; the server `findOrCreate`s.
    - KB links — new `TodoKbEntryPicker.vue` is a multi-select `USelectMenu` with `v-model:search-term` against `GET /api/kb/entries?search=...&limit=10`. Stores ids as `kbEntryIds`. Already-selected ids are kept rendered across queries via a local `id -> title` cache so chips don't flicker when the search narrows.
  - **Deviations:**
    - **Description editor: textarea only, no live preview.** KB editor uses split-pane editor + preview because wikilinks make the preview load-bearing; on a todo description the body is short and the preview buys nothing, so we render only a monospace textarea on the form and a fully-rendered preview on the detail page. Trade documented as a small UX deviation from "match KB editor" — the brief explicitly allows omitting live preview.
    - **Native `<input type="datetime-local">` instead of a Nuxt UI date picker.** Nuxt UI's `UInput type="datetime-local"` was the lowest-friction choice; introducing a calendar widget here is a yak-shave. The form normalises the picker's local-time string to UTC ISO via `new Date(...).toISOString()` before sending to the API.
    - **Detail page included** rather than skipped — the create flow lands on `/app/todos/[id]` (which renders fields + Edit/Complete + KB-link chips + subtask count) so the redirect is natural and T-2.7 has a hook for the subtask UI. Re-using `renderKbMarkdown` keeps wikilink behaviour identical across KB and Todo bodies.

- [x] **T-2.7 — Subtask UI**
  - Nested rendering up to depth 3, with `n/m` progress on parents.
  - Refs: REQ-TODO-2.
  - Done when: Adding/removing subtasks updates the count and the UI renders correctly to depth 3 only.
  - **Implementation:** new recursive `app/features/todo/components/TodoSubtaskList.vue` renders the direct children of a `parentTodoId` via `useTodos({ parentTodoId, includeDeleted: false })`, then mounts itself for each child while `depth < TODO_MAX_DEPTH`. Each row carries a completion checkbox (optimistic via `useCompleteTodo` / `useUncompleteTodo`), the title (deep-link to detail), priority chip, relative due date (red when overdue and active), an inline `n/m` badge, and a "+ Subtask" affordance hidden at depth 3. The recursive subtree mount eagerly loads each level so the tree paints in one go — at our scale and with depth capped at 3 the round-trip count is bounded. The depth gate is purely UX; the server's `TodoSubtaskDepthExceededError` is the authority. Rendering pads `(depth - 1) * 16px` per level.
  - **Subtree fetch — option (a) lazy children:** chosen as the brief allows. Each `TodoSubtaskList` fires one `GET /api/todos?parentTodoId=…` per mounted instance. Pinia-Colada caches the response, so the inline `<TodoSubtaskProgress>` badge per child shares the same cache key as the recursive instance below it (no extra round trip). A recursive CTE that returns the whole subtree in one shot remains a clean future optimisation if subtree sizes grow — pure replacement of `useTodos` calls in two components.
  - **`n/m` semantics:** counts **direct children only** (not the whole subtree). REQ-TODO-2 is silent on this and per-level progress matches the typical UX for nested checklists. The shared `TodoProgressBadge.vue` component takes pre-computed `total` / `completed` so the semantics can be swapped at the call site if a future task wants whole-subtree counts.
  - **"+ Subtask" UX — navigate-with-prefill:** rather than introducing a modal, the affordance links to `/app/todos/new?parentId=<id>` and the existing `TodoForm` accepts a new optional `initialParentTodoId` prop (defaults to `null`; ignored in edit mode). The form fetches the prefilled parent's title via the existing `useTodoById` so the picker chip renders correctly before any keystrokes. Cancel from the prefilled form bounces back to the parent's detail page rather than the flat list. Less code, consistent UX, and the parent picker handles overrides if the user wants to re-target.
  - **Detail page integration** (`app/pages/app/todos/[id]/index.vue`): the prior "subtaskCount placeholder" UCard is replaced by a header showing `<TodoSubtaskProgress>` next to the section title plus a "+ Subtask" UButton, with `<TodoSubtaskList :parent-todo-id="todo.id" :depth="1" />` below it.
  - **List page integration** (`app/pages/app/todos/index.vue`): each row gets `<TodoSubtaskProgress>` next to the priority chip — the component renders nothing when there are zero direct children, so the badge is invisible on flat rows and surfaces only where it adds information. Cost: one tiny per-row query, parallel, Pinia-cached. A server-side aggregation in the list endpoint (with a `?withSubtaskCounts=1` flag) is the cleaner follow-up if page sizes grow; explicitly out of scope here per the "tiny extension" hard limit.
  - **i18n:** new `todo.subtask.*` family in both locales (`add`, `empty`, `error`, `progress.label`, `progress.aria`).
  - **No new tests** — no service / API surface change. Suite stays at 211/211. The brief notes net-new tests only apply if option (b) (recursive CTE service method) is taken; option (a) is purely client-side.
  - **Smoke verify:** logged in as `admin@example.com`, created Parent → Child → Grandchild via `POST /api/todos`. `curl /app/todos/<parent-id>` returned **HTTP 200** with all three titles in the SSR HTML. A `POST /api/todos` with `parentTodoId` set to the grandchild was correctly rejected with **409 `todo.subtask.depth_exceeded` (attemptedDepth 4, maxDepth 3)**, confirming the depth-3 invariant. Smoke fixtures cleaned up via soft-delete + purge.
  - **Files added:** `app/features/todo/components/TodoSubtaskList.vue`, `TodoSubtaskProgress.vue`, `TodoProgressBadge.vue`. **Files modified:** `app/features/todo/components/TodoForm.vue` (added `initialParentTodoId` prop and parent lookup), `app/pages/app/todos/new.vue` (forwards `?parentId` query), `app/pages/app/todos/[id]/index.vue` (replaces placeholder with subtask list + progress + "+ Subtask"), `app/pages/app/todos/index.vue` (per-row badge), `i18n/locales/en.json` + `i18n/locales/de.json` (`todo.subtask.*`).
  - **Quality gates:** `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 211/211.

- [x] **T-2.8 — KB ↔ Todo cross-display**
  - Linked todos appear on the KB entry detail; linked KB entries appear on the todo detail.
  - Refs: REQ-TODO-3.
  - Done when: Linking from either side shows up on both sides.
  - **Implementation:** new server endpoint `GET /api/kb/entries/[id]/linked-todos` (permission `kb:read`, workspace-scoped). Backed by `kbEntryService.getLinkedTodos({ organisationId, entryId, includeCompleted? })` — JOINs `todo_kb_links → todos` filtered to the same workspace, excludes soft-deleted, and excludes completed by default so the KB entry doesn't render a wall of done items. Returns `{ id, title, priority, dueAt, completedAt }`. Sort: `created_at DESC`. Zod schema `linkedTodosQuerySchema` for the optional `includeCompleted` flag. Client side: `useKbLinkedTodos` composable + new "Linked todos" section on the KB detail page below backlinks. Per-row checkbox toggles complete/uncomplete via the existing `useTodoMutations` (extended to also invalidate KB entry queries so the KB-side display refreshes immediately). "Show/hide completed" toggle and "Create todo from this entry" shortcut as required.
  - **Create-from-KB shortcut:** the KB detail page links to `/app/todos/new?kbEntryId=<entryId>`. `TodoForm.vue` accepts a new `initialKbEntryIds?: string[] | null` prop (mirrors T-2.7's `initialParentTodoId`); the create page reads `?kbEntryId` and forwards it. The form uses `useKbEntry(kbLookupId)` to resolve the title for the picker chip in create-mode-from-KB. Cancel from a KB-prefilled form bounces back to the KB entry's slug page.
  - **Deviation — REQ-TODO-3 linking direction:** REQ-TODO-3 only requires *display* of linked KB entries on the todo detail and linked todos on the KB detail — not bidirectional UI for *creating* links. The Todo→KB picker (T-2.6) is the only UI that creates link rows; the KB side is read-only display + the navigate-with-prefill shortcut. Skipped the optional KB-side direct linking modal per the task brief's hard limit.
  - **i18n:** new keys `kb.detail.linkedTodos.{title,empty,createFromEntry,showCompleted,hideCompleted,error}` and `todo.form.create.fromKb.subtitle` in both DE and EN locale files.
  - **New tests:** 3 service tests in `tests/kb-service.test.ts` (`describe('kbEntryService — getLinkedTodos (T-2.8, REQ-TODO-3)')`) covering: (a) linked todos appear, completed excluded by default, included with the flag, soft-deleted always excluded; (b) workspace isolation — querying with a different workspace id returns empty; (c) `created_at DESC` sort. Suite: 211 → 214 green.
  - **Files added:** `server/api/kb/entries/[id]/linked-todos.get.ts`, `app/features/kb/composables/useKbLinkedTodos.ts`. **Files modified:** `server/features/kb/service.ts` (`getLinkedTodos`), `server/features/kb/schemas.ts` (`linkedTodosQuerySchema`), `app/features/kb/api/kb.api.ts` + `kb.keys.ts` + `types/kb.types.ts`, `app/pages/app/kb/[slug].vue` (linked-todos section + toggle + shortcut), `app/features/todo/components/TodoForm.vue` (`initialKbEntryIds` prop + `useKbEntry` lookup), `app/pages/app/todos/new.vue` (`?kbEntryId` query forwarding + KB-aware cancel target), `app/features/todo/composables/useTodoMutations.ts` (cross-feature invalidation of KB entry queries on todo mutations), and both locale JSON files.
  - **Smoke verification:** unit-level — the three new service tests round-trip Todo→KB linking (`todoService.create({ kbEntryIds })`) → KB→Todo display (`kbEntryService.getLinkedTodos()`). The dev DB was empty of admin/RBAC seed during this slot (Nuxt dev server pre-dated a Postgres reset and cannot be restarted from this harness), so a full curl HTTP round-trip was not run. Endpoint route resolves correctly: unauthenticated curl → 401, authenticated without workspace → 403 `workspace.no_membership` (same fallthrough as the rest of the workspace-scoped API). The Todo→KB display side already verified during T-2.6.
  - **Quality gates:** `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 214/214.

### Phase 2 acceptance
- [x] **T-2.X — Phase 2 complete**
  - End-to-end: create todos in different views, with subtasks, link to KB, complete, soft delete, restore.
  - Done when: Manual run-through succeeds; permissions enforced.
  - **Verified:** dev server restarted to re-fire `seedAdmin` (Default Workspace re-created with admin Owner membership). Curl-driven E2E covered every assertion in the brief and all passed cleanly: today/upcoming/open membership + counts `{today:1, upcoming:1, open:3, completed:0}`; subtask depth-2 + depth-3 created, depth-4 rejected with 409 `todo.subtask.depth_exceeded`; KB↔Todo link round-trip (POST 201 → linked-todos includes upcoming → DELETE 204 → linked-todos empty); complete (counts → `today:0, completed:1`) and uncomplete (counts revert); soft delete excludes from default list, restore re-includes; tag round-trip (`POST /tags` 200, detail GET shows `Work`+`Important`, `?tagId=<work>` filter returns the today todo); 401 floor for unauthenticated `POST /api/todos`; cross-workspace isolation confirmed — todo created in WS2 with `X-Organisation-Id` header has the correct `organisationId` and is invisible from WS1's list (and vice versa). Baseline gates green: lint clean, typecheck clean, 214/214 tests passing. No bug fixes required during the run.

---

## Phase 3 — Orchestrator

### Schema
- [x] **T-3.1 — Drizzle schema for orchestrator and AI**
  - Tables: `orchestrator_conversations`, `orchestrator_messages`, `orchestrator_actions`, `ai_providers`, `ai_provider_credentials`, `ai_models`, `orchestrator_workspace_settings`.
  - `orchestrator_conversations.model_id` and `orchestrator_actions.model_id` set up with FK to `ai_models`.
  - Refs: DESIGN-DATA §Orchestrator + §AI Provider.
  - Migration: `0005_flashy_mandarin.sql`. Verified all 7 tables present via `\dt`. pgEnums: `orchestrator_message_role`, `orchestrator_conversation_mode`, `orchestrator_action_class`, `orchestrator_action_status`. Tests still 214/214.

- [x] **T-3.1b — Seed AI providers and initial models**
  - Seed `ai_providers` rows for Anthropic, OpenAI, Google.
  - Seed `ai_models` with a small curated list of currently-recommended frontier models per provider; mark one as `is_default=true`.
  - The seed file (`server/database/seed/ai-models.ts`) is expected to be updated over time as models evolve.
  - Refs: DESIGN-AI §Seed data, REQ-AI-1, REQ-AI-3.
  - Done when: Fresh DB has providers and models; admin UI (T-3.x.UI below) lists them.
  - **Seeded** (`server/database/seed/ai-models.ts`, wired into `server/plugins/01.init.ts`):
    - Providers: `anthropic`, `openai`, `google` (all `enabled=true`).
    - Anthropic models: `claude-opus-4-7`, **`claude-sonnet-4-6` (`is_default=true`)**, `claude-haiku-4-5`.
    - OpenAI models: `gpt-5.5`, `gpt-5.4`, `gpt-5.4-mini`.
    - Google models: `gemini-3.1-pro`, `gemini-2.5-pro`, `gemini-2.5-flash`.
    - All rows: `supports_tools = supports_streaming = supports_vision = true`. Anthropic rows include `input/output_price_per_mtok` per the public pricing page; OpenAI/Google price columns left null pending the rest of T-3.x.
    - Verified on the dev DB (`docker exec accio-postgres-db psql -U postgres -d accio …`): `ai_providers` returns 3 rows; `ai_models` returns the 9 rows above with exactly one `is_default=true`. Seed is idempotent — re-runs log "All providers and models already present".
  - **Verification:** model IDs cross-checked against Anthropic, OpenAI, and Google docs (May 2026). Anthropic alias-form IDs (`claude-opus-4-7`, `claude-sonnet-4-6`, `claude-haiku-4-5`) used; if a future task wants pinned-snapshot IDs, the admin UI can add them alongside.
  - Quality gates: `pnpm typecheck` green, `pnpm test:run` 214/214. Pre-existing `pnpm lint` errors in `server/database/migrations/meta/{0005_snapshot.json,_journal.json}` (`style/eol-last`) inherited from T-3.1 — orthogonal to this task; flagged but not fixed here.

### AI provider abstraction
- [x] **T-3.1c — Install Vercel AI SDK and provider packages**
  - Add deps: `ai`, `@ai-sdk/anthropic`, `@ai-sdk/openai`, `@ai-sdk/google`.
  - Refs: DESIGN-AI.
  - **Implementation notes:**
    - Already present from earlier scaffolding: `ai@6.0.116`, `@ai-sdk/anthropic@3.0.58`, `@ai-sdk/google@3.0.43`, `@ai-sdk/vue@3.0.116`.
    - Newly added in this task: `@ai-sdk/openai@3.0.60` (catalog entry `^3.0.60`, referenced via `catalog:` in `package.json`, matching the pattern of the other AI SDK packages).
    - Smoke-tested imports of `generateText` (from `ai`), `anthropic`, `openai`, `google` resolve under `pnpm typecheck`; smoke file reverted before commit.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 214/214.

- [x] **T-3.1d — Provider client builder**
  - `server/features/ai/provider.ts`: given a `model_id`, resolves provider, fetches and decrypts credentials for the active workspace, returns a ready-to-use AI SDK model instance.
  - Caches nothing across requests (DESIGN-AI: avoid stale-key issues).
  - Refs: DESIGN-AI §Provider-Model resolution flow.
  - Done when: Unit test with fixture credentials produces a working SDK client for each of Anthropic / OpenAI / Google (mocked transport).
  - **Implementation notes:**
    - Service: `server/features/ai/provider.ts` — `createAiProviderService({ db })` exposing `resolveModelClient({ organisationId, modelId })` and `getDefaultModel({ organisationId })`. Errors live in `server/features/ai/errors.ts`: `AiModelNotFoundError`, `AiModelDisabledError`, `AiProviderDisabledError`, `AiProviderUnsupportedError`, `AiCredentialsMissingError`, `AiNoDefaultModelError`.
    - Resolution flow mirrors DESIGN-AI: load `ai_models ⨝ ai_providers` by model id, validate `enabled` on both, load `ai_provider_credentials` for `(organisation_id, provider_id)`, look up org `crypto_salt`, `decryptForOrg(api_key_encrypted, crypto_salt)`, then dispatch on `ai_providers.key`: `anthropic → createAnthropic({ apiKey })(model.modelId)`, `openai → createOpenAI(...)`, `google → createGoogleGenerativeAI(...)`. Returns a `LanguageModelV3` plus the originating `ai_models` row and provider key.
    - **No request-level caching** — each call re-queries the DB and re-builds the SDK client. Test asserts three identical calls produce three `createAnthropic` invocations.
    - `getDefaultModel` fallback chain: `orchestrator_workspace_settings.default_model_id` (only if the target row is enabled) → `ai_models WHERE is_default = true AND enabled = true` → throws `AiNoDefaultModelError`.
    - Wired into `server/utils/container.ts` as `aiProviderService`.
    - **Schema/seed naming deviation from the task brief:** the task description used `provider.code` / `model.code` / `is_enabled`; the actual schema uses `ai_providers.key`, `ai_models.modelId`, and `enabled` (per T-3.1 / T-3.1b). Implementation follows the schema.
    - **Tests:** `tests/ai-provider.test.ts` — 13 tests. Mocks `@ai-sdk/anthropic`, `@ai-sdk/openai`, `@ai-sdk/google` via `vi.mock` + `vi.hoisted` so each provider call returns a sentinel `{ provider, modelId }` and we can assert (a) the factory was called with the decrypted `apiKey`, (b) the provider object was called with the provider-side model id. AI tables aren't covered by the global `cleanDatabase` TRUNCATE, so the suite truncates `ai_providers`, `ai_models`, `ai_provider_credentials`, and `orchestrator_workspace_settings` itself in `beforeEach`.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 227/227 (was 214; +13).

- [x] **T-3.1e — AI configuration API + UI**
  - Routes per DESIGN-API §AI Configuration.
  - Pages: `/app/settings/ai` (workspace credentials, default model, AI display name, history limit) and `/admin/ai/models` (super-admin model registry CRUD — stays under `/admin` because it's platform-global data).
  - Permission guards: `ai:read` for GET, `ai:manage` for PUT/PATCH/DELETE on workspace endpoints; `platform:admin` for `/admin/ai/models`.
  - Saving an API key encrypts it via `encryptForOrg`; reading credentials never returns the plaintext.
  - Refs: REQ-AI-2, REQ-AI-3, REQ-AI-4, DESIGN-API §AI Configuration.
  - **Implementation notes:**
    - Workspace routes (`/api/ai/...`): `providers.get` + `models.get` + `credentials/index.get` + `credentials/[providerId].put` + `credentials/[providerId].delete` + `settings.get` + `settings.put` + `settings.patch` (PATCH alias of PUT for clients that prefer either verb). Permission guards via `requirePermission(AI_READ | AI_MANAGE)` scoped to the workspace organisation.
    - Admin routes (`/api/admin/ai/...`): `providers/index.get`, `providers/[id].patch` (toggle enabled), `models/index.get`, `models/index.post`, `models/[id].patch`, `models/[id].delete`. All gated by `requireSuperAdmin` (matches the rest of `/admin`). 11 route files total (8 workspace + 6 admin minus the shared `[providerId]` pair = 14 file count).
    - Schemas: `server/features/ai/schemas.ts` (Zod v4). Shared error mapping in `server/features/ai/api-utils.ts` — codes: `validation.failed`, `ai.model.not_found`, `ai.model.disabled`, `ai.provider.disabled`, `ai.provider.not_found`, `ai.provider.unsupported`, `ai.credentials.missing`, `ai.credentials.conflict`, `ai.model.conflict`, `ai.default_model.missing`. Two new errors added to `server/features/ai/errors.ts`: `AiUniqueConflictError`, `AiProviderNotFoundError`.
    - Service surface extended on `aiProviderService` (in `server/features/ai/provider.ts`): `listProviders`, `listModels`, `listWorkspaceCredentialStatus`, `setWorkspaceCredentials`, `clearWorkspaceCredentials`, `getWorkspaceSettings` (create-on-read with `config.orchestrator.historyLimit` default), `setWorkspaceSettings`, `createModel`, `updateModel`, `deleteModel`, `updateProvider`. `is_default = true` toggles use a transaction to unset the previous default in the same statement. Unique-violation detection walks the cause chain (Drizzle wraps the pg-error).
    - Plaintext key handling: `setWorkspaceCredentials` encrypts via `encryptForOrg` inline (no intermediate variable persists past the call). The encrypted blob is stored in `ai_provider_credentials.api_key_encrypted` only; no list / status endpoint returns it. Asserted by a test that JSON-stringifies the credential-status response and grep-checks for both the plaintext key and the column name.
    - Client slice: `app/features/ai/{api,composables,types}` with `useAiProviders`, `useAiModels`, `useAiCredentials`, `useAiSettings`, `useSetAiCredential`, `useClearAiCredential`, `useUpdateAiSettings`, plus the admin variants in `useAdminAiModels.ts`.
    - Pages: `app/pages/app/settings/ai.vue` (three sections: display + history, default model picker scoped to providers with credentials per REQ-AI-4, provider credential list with set/clear modals — never previewing the existing key) and `app/pages/admin/ai/models.vue` (provider toggles + models table with inline enabled/default switches, edit/delete, and a create-or-edit modal). Both use the existing `app` / `admin` layouts.
    - Permission gating: workspace page uses `usePermissions().isGlobalAdmin` + a "has `ai:manage` somewhere" fallback (no active-workspace-id composable yet — the server is still the source of truth via `requirePermission`). Admin page uses the existing `admin` middleware (platform-admin only).
    - i18n: full `ai.settings.*` and `ai.admin.*` blocks in both `de.json` and `en.json`.
    - **Tests:** `tests/ai-config.test.ts` — 14 new tests covering credential round-trip (encrypted blob is not the plaintext, `decryptForOrg` recovers it, status response carries no key-derived fields), credential upsert, clear, workspace settings create-on-read + happy path + unknown-model rejection + disabled-model rejection, default-model toggle on create AND update unsets the prior default, unique-conflict on duplicate model, list filters (`enabled` / `includeDisabled`), and `deleteModel` removes the row.
    - **Smoke transcript** (logged in as the seed admin against the Default Workspace):
      - `GET /api/ai/providers` → 200, three providers, all `hasCredentials: false`.
      - `PUT /api/ai/credentials/<anthropic-id>` with `{apiKey: "sk-test-123"}` → 200 `{ credential: { providerId, hasCredentials: true } }`.
      - `GET /api/ai/credentials` → Anthropic now `hasCredentials: true`, others false.
      - `PUT /api/ai/settings` with `{historyLimit: 50, aiDisplayName: "Aria"}` → 200; subsequent `GET` reflects.
      - `PUT /api/ai/credentials/<google-id>` with `{apiKey: ""}` → 400 `validation.failed` (Zod `too_small`).
      - `GET /api/admin/ai/models` as platform admin → 200 with the seeded models.
      - `GET /app/settings/ai` (cookie) → 200; `GET /admin/ai/models` (cookie) → 200; `GET /admin/ai/models` (no cookie) → 302 redirect (auth gate).
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 241/241 (was 227; +14).
  - **Deviation:** DESIGN-API spec lists `/api/ai/workspace-settings`; the implementation exposes the route at `/api/ai/settings` (matching the DB table and shorter URL), with both PUT and PATCH verbs accepted. Provider POST/DELETE were intentionally skipped (ADR-013 — adding a provider requires a bundled SDK adapter), only `PATCH /api/admin/ai/providers/[id]` for the enabled toggle is exposed. `DELETE /api/admin/ai/models/[id]` is hard delete; FK-violation detection against `orchestrator_actions` is deferred until T-3.7 lands the audit-write path.

### Self-MCP server
- [x] **T-3.2 — In-process MCP server bootstrap**
  - `server/features/orchestrator/mcp-server.ts` exposes a tool registry: each tool is `{name, description, schema, class, handler}`.
  - In-process invocation only — no HTTP transport (ADR-006).
  - Refs: REQ-ORCH-2, ADR-006.
  - Done when: A test spins up the registry and invokes a stub tool through it.
  - **Implementation notes:**
    - Files: `server/features/orchestrator/mcp-server.ts` (registry + factory), `server/features/orchestrator/errors.ts` (typed error classes).
    - Exported types from `mcp-server.ts`: `Tool<I, O>`, `ToolClass` (`'auto' | 'confirm'`), `ToolMode` (`'read' | 'write'`), `McpToolContext` (`{organisationId, userId, conversationId?, mode: 'read_only' | 'read_write'}`), `McpToolResult<T>`, `ListFilter`, `McpServer` (registry interface), and the `createMcpServer()` factory. Registry surface: `register`, `list({mode?})`, `has`, `get`, `invoke`.
    - Exported errors from `errors.ts`: `McpToolError` (base), `McpToolNotFoundError`, `McpToolInputError` (carries Zod `issues`), `McpToolExecutionError` (wraps unexpected handler throws with original on `.cause`), `McpToolDuplicateError`. `invoke` lets any `McpToolError` subclass thrown from a handler bubble out unchanged so T-3.6's `ConfirmationRequired` won't be swallowed.
    - Tool contract follows DESIGN-TOOLS shape (`name`, `description`, `input_schema` (Zod v4), `class`, `read_or_write`) with the `read_or_write` field named `mode` in the type to avoid the keyword-style separator. ADR-006 honoured: in-process only, MCP-shaped, no transport.
    - Conversation `mode` is captured on `McpToolContext` but not yet enforced against `Tool.mode` inside `invoke` — the chat handler in T-3.11 will only register read tools for `read_only` conversations (per REQ-ORCH-3), so a runtime cross-check is redundant for now. Easy to add later if needed.
    - No singleton wired into `server/utils/container.ts` — orchestrator chat handler will instantiate per-request (TODO marker left in `mcp-server.ts` for T-3.11).
    - **Tests:** `tests/mcp-server.test.ts` — 8 new pure-unit tests: register-and-filter (×2), invoke-with-context, Zod-failure-skips-handler, unknown-tool, duplicate-name, plain-Error wrapping with `.cause` preservation, and McpToolError-subclass passthrough (uses an in-test `FakeConfirmationRequired` to lock in T-3.6's bubble-out path).
    - **T-3.4 extension:** `Tool.class` widened from `ToolClass` to `ToolClassifier<I> = ToolClass | ((input: I) => ToolClass)` so input-dependent classification (e.g. `kb_create_entry`'s `auto`-when-`inbox`-else-`confirm`) can live on the tool definition. Added `classifyTool(tool, input)` helper for T-3.6's wrapper to resolve uniformly. Also added optional `authorName?: string` to `McpToolContext` for REQ-ORCH-7 author propagation (chat handler T-3.11 will populate from the conversation's selected model display name).
    - **T-3.6 extension:** added optional `Tool.affectedCount?: (input) => number` hook (ADR-010 bulk rule, default 1) and optional `McpToolContext.confirmationToken?: string` (post-confirm bypass signal — registry treats presence/absence as the gate; chat handler owns crypto/expiry/binding). New `McpServer.classify(name, input)` returns `{ effectiveClass, reason, affectedCount }` without invoking, for the chat handler's pre-flight decision.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 249/249 (was 241; +8). No regressions.
  - **Deviation:** none. The contract field DESIGN-TOOLS calls `read_or_write` is exposed on the type as `mode: ToolMode` for ergonomics; semantics are identical.

- [x] **T-3.3 — Read tools**
  - Implement: `kb_search`, `kb_get_entry`, `kb_list_categories`, `kb_list_tags`, `todo_search`, `todo_get`. Project-related read tools deferred to Phase 4.
  - Refs: DESIGN-TOOLS §Read.
  - Done when: Each tool has a unit test that exercises it via the registry.
  - **Implementation notes:**
    - Files: `server/features/orchestrator/tools/{kb-search,kb-get-entry,kb-list-categories,kb-list-tags,todo-search,todo-get}.ts` plus `tools/index.ts` (composite `createReadTools`, `registerReadTools`, type re-exports).
    - Each tool factory exports `createXxxTool(deps): Tool<I, O>` with input/output types and Zod input schema co-located in the slice file (per DESIGN-ARCH "one file per tool"). All six tools are `class: 'auto'`, `mode: 'read'`. String inputs use `.trim()` (Zod v4); workspace scoping is via `ctx.organisationId` only — no client-supplied org ids.
    - Errors: added `McpToolNotFoundEntityError` to `errors.ts` (distinct from `McpToolNotFoundError` for unknown tool names) — thrown by `kb_get_entry` / `todo_get` for missing rows or cross-workspace lookups; bubbles unchanged through `invoke` because it extends `McpToolError`.
    - **Service extensions** (minimal, each commented with the tool that needs it):
      - `kbEntryService.search(...)` — FTS path that returns rows annotated with `ts_rank_cd`. Mirrors the existing `list` FTS branch but exposes the rank score and supports an AND-tag filter set. Needed by `kb_search`.
      - `kbTagService.listWithUsage(...)` — LEFT JOIN on `kb_entry_tags` so tags are returned with their entry-usage count (zero-usage tags included). Needed by `kb_list_tags`.
    - Deviations: KB tags have no stored slug column (ADR-008), so `kb_list_tags` derives the slug via the existing `slugify` helper for the spec's `slug` output field. `todo_search` accepts multiple tag names and ANDs them in JS over the canonical view's result (`todoService.list` exposes a single-tag filter only).
    - **Tests:** `tests/mcp-read-tools.test.ts` — 20 DB-backed integration tests, every tool invoked through `server.invoke(name, …)`. Coverage: registry registration (six tools, all auto/read, zero write), kb_search happy path / workspace isolation / default status excludes inbox+archived / explicit status override / tag narrowing / unknown tag short-circuits / limit cap rejected by schema, kb_get_entry slug + id resolution / not-found / cross-workspace not-found / backlinks, kb_list_categories tree shape / workspace isolation, kb_list_tags usage-count sort / cross-workspace isolation, todo_search default+today views / linked_to_kb (slug or id) / priority / cross-workspace, todo_get hydrated row + subtasks + linked KB / not-found / cross-workspace.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 269/269 (was 249; +20). No regressions.
  - Container: nothing wired into `server/utils/container.ts` per the brief — T-3.11 will instantiate the registry per-request and call `registerReadTools`.

- [x] **T-3.4 — Write tools (KB)**
  - Implement: `kb_create_entry`, `kb_update_entry`, `kb_set_status`, `kb_add_tag`, `kb_remove_tag`, `kb_link_entries`, `kb_soft_delete_entry`.
  - Each tool's handler returns a structured result and never hard-deletes.
  - Author propagation per REQ-ORCH-7.
  - Refs: DESIGN-TOOLS §Write KB, REQ-ORCH-7, REQ-ORCH-8.
  - **Implementation notes:**
    - Files: `server/features/orchestrator/tools/{kb-create-entry,kb-update-entry,kb-set-status,kb-add-tag,kb-remove-tag,kb-link-entries,kb-soft-delete-entry}.ts` plus shared helper `tools/kb-resolve-entry.ts` (slug-or-id → live entry within the workspace, throws `McpToolNotFoundEntityError` otherwise). `tools/index.ts` extended with `createWriteToolsKb`, `registerWriteToolsKb`, and a composite `createAllTools(deps): { read, writeKb }` for T-3.11 — keys forward-compatible with `writeTodos` (T-3.5) and `projects` (Phase 4).
    - Each tool factory exports `createXxxTool(deps): Tool<I, O>` with input/output types and Zod v4 input schema co-located. All seven are `mode: 'write'`. Class declarations: `kb_create_entry` uses a function classifier (`auto` when `status='inbox'`/omitted, `confirm` for `draft`/`verified`) — exported as `classifyKbCreateEntry` for T-3.6 to reuse; `kb_update_entry`, `kb_set_status`, `kb_soft_delete_entry` are static `confirm`; `kb_add_tag`, `kb_remove_tag`, `kb_link_entries` are static `auto`. None hard-delete; `kb_soft_delete_entry` only flips `deleted_at`.
    - **Author propagation (REQ-ORCH-7):** `kb_create_entry` always sets `authorType='ai'`, `authorName = ctx.authorName ?? 'AI'`, `sourceType = input.source_type ?? 'chat'`, `sourceRef = input.source_ref ?? ctx.conversationId ?? null`, and `createdBy = ctx.userId`. Tool args do NOT carry author fields. T-3.11 must populate `ctx.authorName` from the conversation's selected model display name.
    - **Context extension:** `McpToolContext` gained an optional `authorName?: string` (see T-3.2 notes). Existing read-tool call sites need no change — write tools default to `'AI'` when missing.
    - **Tool.class extension:** widened to `ToolClassifier<I> = ToolClass | ((input: I) => ToolClass)` (see T-3.2 notes). Resolved via `classifyTool(tool, input)` from `mcp-server.ts`. T-3.6's confirmation wrapper will call `classifyTool` to decide whether to short-circuit; tests use the same helper.
    - **Slug/id resolution:** all write tools that accept `slug_or_id` route through `resolveLiveKbEntry` — id lookup is gated on the same ULID heuristic as `kb_get_entry`, soft-deleted rows are treated as not-found, and cross-workspace ids/slugs surface as `McpToolNotFoundEntityError` (no leak, no modify).
    - `kb_link_entries` uses an `appendWikilink` helper that re-inserts `[[target-slug]]` with a single blank-line separator, idempotent on repeat (no-op when the link already exists). It then calls `kbEntryService.update({ body })`, which owns the wikilink-rebuild transaction (DESIGN-WIKILINKS step 2) — the tool deliberately does not touch `kb_entry_links` directly.
    - `kb_add_tag` reuses `kbTagService.findOrCreate` (matches the existing API route's behaviour); `kb_remove_tag` looks up the tag by case-insensitive name and is a no-op when missing.
    - **Tests:** `tests/mcp-write-tools-kb.test.ts` — 20 DB-backed integration tests; every write tool invoked through `server.invoke(name, …)`. Coverage: registry registration (seven write tools, mode=write), static-class assertions for the six static tools, function-classifier for `kb_create_entry` (auto for inbox/omitted, confirm for draft/verified), kb_create_entry happy path with author propagation / category resolution / tag attach / `authorName` default to `'AI'` / explicit draft / unknown category 404, kb_update_entry happy path / not-found / cross-workspace refusal, kb_set_status valid transition / schema-rejected bogus status, kb_add_tag auto-create / round-trip add+remove with case-insensitive match / remove no-op, kb_link_entries one_way / both, kb_soft_delete_entry sets `deleted_at` and subsequent kb_get_entry returns 404 / cross-workspace refusal (entry stays live), error wrapping (McpToolNotFoundEntityError bubbles, NOT wrapped in McpToolExecutionError). Also extended `tests/mcp-server.test.ts` with one new test locking the `Tool.class` static-or-function contract via `classifyTool`.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 290/290 (was 269; +21). No regressions.
  - Container: nothing wired into `server/utils/container.ts` per the brief — T-3.11 will instantiate the registry per-request and call `registerWriteToolsKb` for `read_write` conversations.

- [x] **T-3.5 — Write tools (Todos)**
  - Implement: `todo_create`, `todo_update`, `todo_complete`, `todo_uncomplete`, `todo_soft_delete`, `todo_link_kb`, `todo_unlink_kb`.
  - Refs: DESIGN-TOOLS §Write Todos.
  - **Implementation notes:**
    - Files: `server/features/orchestrator/tools/{todo-create,todo-update,todo-complete,todo-uncomplete,todo-soft-delete,todo-link-kb,todo-unlink-kb}.ts` plus shared helper `tools/todo-resolve.ts` (id → live todo within the workspace, throws `McpToolNotFoundEntityError` otherwise — mirrors `kb-resolve-entry.ts`). `tools/index.ts` extended with `createWriteToolsTodo`, `registerWriteToolsTodo`, and the composite `createAllTools(deps)` now returns `{ read, writeKb, writeTodos }` (forward-compatible with `projects` Phase 4).
    - Each tool factory exports `createXxxTool(deps): Tool<I, O>` with input/output types and Zod v4 input schema co-located. All seven are `mode: 'write'`. Class declarations: `todo_create`, `todo_update`, `todo_complete`, `todo_uncomplete`, `todo_link_kb`, `todo_unlink_kb` are static `auto`; `todo_soft_delete` is static `confirm`. None hard-delete; `todo_soft_delete` only flips `deleted_at`.
    - **Author propagation:** `todos` schema has no `author_type` / `author_name` columns (only `created_by`), so the tools record `createdBy = ctx.userId` and do NOT use `ctx.authorName`. KB write tools remain the only consumers of that ctx field.
    - **KB slug-or-id resolution:** `todo_create` (for the `kb_links: string[]` initial-link set) and `todo_link_kb` / `todo_unlink_kb` route through the existing `resolveLiveKbEntry` helper, so cross-workspace KB references and soft-deleted entries surface as `McpToolNotFoundEntityError` (no leak, no modify). `todoService.linkKb` / `unlinkKb` are idempotent — re-linking returns the same shape; `todo_unlink_kb` returns `{removed: false}` when the link did not exist.
    - **Service surface:** all write tools call existing methods on `todoService` (`create`, `update`, `complete`, `uncomplete`, `softDelete`, `linkKb`, `unlinkKb`, `findById`); no new service methods were added. ISO-string `due_at` inputs are coerced to `Date` in the handler before delegating; bogus dates throw a plain `Error` which `invoke` wraps in `McpToolExecutionError`.
    - Deps for `createWriteToolsTodo`: `{ todoService, kbEntryService }` — `kbEntryService` is needed for KB slug resolution. No `kbTagService` needed (tag rebuilds happen entirely inside `todoService`).
    - **Tests:** `tests/mcp-write-tools-todo.test.ts` — 19 DB-backed integration tests; every write tool invoked through `server.invoke(name, …)`. Coverage: registry registration (seven todo write tools, mode=write), static-class assertions for all seven, `todo_create` happy path with priority/due_at/created_by stamping / parent_todo_id resolution / tag attach / mixed slug+id `kb_links` resolution / unknown-slug 404 / cross-workspace KB slug refusal (no row created), `todo_update` happy path / not-found / cross-workspace refusal, `todo_complete` + `todo_uncomplete` round-trip / cross-workspace complete refused, `todo_soft_delete` static-class confirm / sets deleted_at and subsequent `todo_get` returns 404 / cross-workspace refusal (todo stays live), `todo_link_kb` / `todo_unlink_kb` idempotent round-trip / cross-workspace KB slug 404 (no row) / cross-workspace todo id 404.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 309/309 (was 290; +19). No regressions.
  - Container: nothing wired into `server/utils/container.ts` per the brief — T-3.11 will instantiate the registry per-request and call `registerWriteToolsTodo` for `read_write` conversations.

- [x] **T-3.6 — Tool classification and confirmation enforcement**
  - Each tool declares `class`. `confirm`-class tools throw a structured `ConfirmationRequired` from the registry; the chat handler surfaces it to the client.
  - Bulk-change rule (≥6 affected items): enforced via a wrapper applied to all write tools.
  - Refs: REQ-ORCH-4, DESIGN-CONF.
  - Done when: Test verifies that `kb_update_entry` triggers a confirmation request and only executes after explicit resume.
  - **Implementation notes:**
    - **New error in `errors.ts`:** `ConfirmationRequiredError` extends `McpToolError`, carries `{ toolName, toolClass: 'confirm', input, affectedCount, reason: 'class' | 'bulk' }`. Bubbles unchanged through `invoke` (subclass passthrough already in place from T-3.2). The chat handler (T-3.11) catches by class, persists a pending tool call, and serialises into the SSE `confirmation_required` event.
    - **`Tool.affectedCount?: (input: I) => number` hook** added to the tool contract for ADR-010's bulk override. Defaults to `1` when omitted. None of the current tools opt in (no bulk-array writes today). `BULK_CONFIRM_THRESHOLD` constant set to `6` per DESIGN-CONF.
    - **`McpToolContext.confirmationToken?: string`** added as the bypass signal. Presence/absence is the gate at the registry layer; the chat handler decides cryptographic strength, expiry, and binding (documented on the field).
    - **`invoke` wrapper logic:** after Zod validation, compute `effectiveClass` via `classifyTool(tool, input)`, then auto-promote to `'confirm'` with `reason='bulk'` when `affectedCount(input) >= 6`. If `effectiveClass === 'confirm'` and `ctx.confirmationToken` is missing → throw `ConfirmationRequiredError` (handler not called). Otherwise run handler as before. Existing `McpToolError`-subclass passthrough preserved.
    - **`McpServer.classify(name, input)`** convenience method returns `{ effectiveClass, reason, affectedCount }` without invoking. Exposed for the chat handler's pre-flight check (decide whether to start the SSE confirm flow without throwing). Shares `resolveAndParse` + `resolveClassification` helpers with `invoke`.
    - **Existing tests touched:** `tests/mcp-server.test.ts` `FakeConfirmationRequired` test now passes a `confirmationToken` so it still asserts handler-thrown subclass passthrough (rather than tripping the new gate). `tests/mcp-write-tools-kb.test.ts` and `tests/mcp-write-tools-todo.test.ts` updated: every invocation of a `confirm`-class tool (`kb_update_entry`, `kb_set_status`, `kb_soft_delete_entry`, `todo_soft_delete`, `kb_create_entry` with `status='draft'`) now passes `confirmationToken: 'test-confirmed'` on the ctx — these tests exercise post-confirm execution paths, not the gate itself.
    - **Tests:** `tests/mcp-confirmation.test.ts` — 7 new pure-unit tests covering: auto-class runs without token; static `confirm` throws with `reason='class'` and validated input; confirm with token runs; dynamic classifier (`kb_create_entry`-shaped fixture) — `inbox` skips, `verified` requires; bulk override (`affectedCount=5` runs, `=6` throws with `reason='bulk'`); bulk with token runs; `server.classify` returns the same triple without throwing across all three branches. Plus 1 new DB-backed integration test in `tests/mcp-write-tools-kb.test.ts` exercising the spec's verbatim "Done when": `kb_update_entry` first call throws `ConfirmationRequiredError` (entry untouched, validated input on the error), second call with `confirmationToken` performs the update (verified by row read-back).
    - **Deviations:** none. No changes to `Tool.class` typing or `classifyTool` signature. No `pending_tool_call` table here — that's T-3.7 (audit) / T-3.11 (chat handler). No singleton wired into `server/utils/container.ts`.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 317/317 (was 309; +8). No regressions.

### Audit log
- [x] **T-3.7 — Audit log writes**
  - Wrapper around tool execution writes to `orchestrator_actions` for every executed call (auto and confirmed).
  - Refs: REQ-ORCH-6, REQ-AI-5, DESIGN-CHAT step 5, ADR-010.
  - **Implementation notes:**
    - File: `server/features/orchestrator/audit.ts`. Sits next to `mcp-server.ts` so the registry stays pure-structural (no DB import). `mcp-server.ts` is **unchanged** by this task — audit is a wrapper layer, picked design (a) from the brief.
    - Exports: `createAuditService({ db }): AuditService` with surface `recordPending`, `recordExecuted`, `recordFailed`, `recordCancelled`, `markConfirmed`, `attachMessageId`, plus internal `patchExecuted` / `patchFailed` (used by `auditedInvoke` to keep one row per logical call across the pending → confirmed → executed/failed transitions). Also exports the higher-order `auditedInvoke({ server, audit, name, input, ctx, modelId, priorActionId? })` and the result types `AuditedInvokeOutcome<O> = AuditedInvokeExecuted<O> | AuditedInvokeConfirmationRequired`.
    - **`auditedInvoke` envelope** (so T-3.11 / T-3.12 can plug in):
      - `{ kind: 'executed', actionId, result }` — handler ran. `actionId` is `''` for read tools (no row written).
      - `{ kind: 'confirmation_required', actionId, toolName, input, affectedCount, reason }` — gate fired, `pending_confirmation` row inserted; the chat handler emits SSE `confirmation_required` from this directly. T-3.12's confirm endpoint passes back `priorActionId: actionId` plus `ctx.confirmationToken` to re-enter.
      - Failures: handler throws → row recorded as `failed` (or prior row patched to `failed`), then **re-thrown unchanged** so the chat handler can pattern-match `McpToolError` subclasses (e.g. `McpToolNotFoundEntityError`) the same way as raw `invoke`.
    - **Read tools are a no-op** — REQ-ORCH-6 only mandates audit for write tools. `auditedInvoke` short-circuits via `tool.mode === 'read'` before any DB write.
    - **Bulk-rule recording (ADR-010):** when the call is auto-promoted (`reason='bulk'`), the row records `class: 'auto'` (the *declared* class) so the audit log shows "this would normally have been auto, but the bulk rule fired". Intrinsic-confirm tools record `class: 'confirm'`. `affectedCount` is always populated via `tool.affectedCount?.(input) ?? 1`.
    - **Re-entry path:** when `priorActionId` is supplied (T-3.12), `auditedInvoke` calls `markConfirmed(priorActionId)` then patches the same row to `executed`/`failed` rather than inserting a second row — single audit row per logical call.
    - **Container wiring:** `server/utils/container.ts` exposes `container.auditService` via the `lazy(() => createAuditService({ db: getDatabase('app') }))` factory (mirrors `aiProviderService`). The chat handler (T-3.11) will resolve from there.
    - **Schema deviation called out in brief:** the design table mentions a `confirmed: boolean` column, but the implemented schema (`server/database/schema/orchestrator-actions.ts`) uses an `orchestrator_action_status` pgEnum (`pending_confirmation` | `confirmed` | `cancelled` | `executed` | `failed`). Audit writes target the enum — schema is authoritative; spec table is stale. `class` mirrors the `orchestrator_action_class` pgEnum (`auto` | `confirm`).
    - **Field population:** id (ulid), organisationId, conversationId, userId, modelId, toolName, parameters (validated post-Zod input — JSON), class, status, affectedCount, plus the appropriate timestamps per state (`createdAt` defaults; `confirmedAt`, `cancelledAt`, `executedAt` set on transition); `result` on executed; `error` on failed; `messageId` defaults null and is late-bound via `attachMessageId` (T-3.11).
    - **Tests:** `tests/orchestrator-audit.test.ts` — 10 DB-backed tests, every assertion reads back the actual `orchestrator_actions` row(s). Coverage:
      - Auto write tool: one `executed` row with full field shape (`toolName`, `parameters`, `class='auto'`, `status='executed'`, `executedAt` populated, `result` populated, `modelId`, `userId`, `conversationId`, `organisationId` correct; `messageId`/`error`/`confirmedAt`/`cancelledAt` null).
      - Read tool: no row written, `actionId === ''`.
      - Confirm tool without token: `pending_confirmation` row with `class='confirm'`, handler not called, envelope returned with `reason='class'`.
      - Confirm tool with token + `priorActionId`: same row patched to `executed`, `confirmedAt` and `executedAt` populated; **only one row total**.
      - Handler throws (`McpToolNotFoundEntityError`): row recorded as `failed` with `error` populated, error re-thrown unchanged.
      - Bulk override: `affectedCount=7`, no token → row with `class='auto'`, `status='pending_confirmation'`, `affectedCount=7`. After re-invoke with token: same row to `executed`, `class='auto'` preserved, `affectedCount=8` populated.
      - `attachMessageId`: updates `messageId`. `recordCancelled`: flips `pending_confirmation` → `cancelled` with `cancelledAt`. `markConfirmed`: flips → `confirmed` with `confirmedAt`.
      - Test setup explicitly truncates `orchestrator_actions, orchestrator_messages, orchestrator_conversations, ai_models, ai_providers` in `beforeEach` (same pattern as the AI tests) — these aren't covered by `tests/setup.ts` global TRUNCATE.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 327/327 (was 317; +10). No regressions.
  - **Deviations:** none beyond the documented `confirmed boolean → status enum` schema reality. No API routes here (T-3.8 covers GET, T-3.11 wires audit calls into the chat flow, T-3.12 covers confirm/cancel). No new permissions (T-3.8 will add `orchestrator:audit:view`).

- [x] **T-3.8 — Audit log API + UI (admin)**
  - `GET /api/orchestrator/audit` with pagination and filters; UI page under `/app/settings/audit` (workspace-scoped, gated by `orchestrator:audit:view`).
  - Permission: `orchestrator:audit:view` (already exists from DESIGN-RBAC; Owner-only per `rbac.seed.ts`).
  - Refs: REQ-ORCH-6, DESIGN-RBAC.
  - **Implementation notes:**
    - **Service extension:** `server/features/orchestrator/audit.ts` gained read-side `list({ organisationId, filter?, pagination?, sort? })` and `findById(actionId, organisationId)` methods returning `AuditRowHydrated` with `model: { id, displayName, modelId } | null` (LEFT JOIN on `ai_models`, tolerant of FK SET NULL) and `conversationTitle` (LEFT JOIN on `orchestrator_conversations`). `total` comes from a separate `count(*)::int` over the same WHERE clause. Constants `AUDIT_LIST_DEFAULT_LIMIT=50` and `AUDIT_LIST_MAX_LIMIT=200` exported.
    - **Filters:** `conversationId` (exact), `toolName` (LIKE substring), `modelId` (exact), `status` (multi via `inArray`), `class` (multi via `inArray`), `since`/`until` (`gte`/`lte` on `created_at`). Sort default `createdAt:desc` with stable secondary `desc(id)` so paginated results are deterministic.
    - **HTTP routes:**
      - `GET /api/orchestrator/audit` → `{ rows, total, limit, offset }`.
      - `GET /api/orchestrator/audit/[id]` → `{ row }`; cross-org id returns 404 `orchestrator.audit.not_found` (no existence leak).
      - Both gated by `requirePermission(ORCHESTRATOR_AUDIT_VIEW)` against `resolveWorkspace(event).organisationId`.
    - **Validation plumbing:** `server/features/orchestrator/api-utils.ts` (new — modelled on `server/features/ai/api-utils.ts`) exports `orchestratorThrow`, `orchestratorValidate`, `readOrchestratorQuery`, `getRequiredParam`. `server/features/orchestrator/audit-schemas.ts` (new) holds the Zod v4 query schema (`auditListQuerySchema`) with `repeatableEnum` preprocessor (collapses URLSearchParams string-or-array to array), `isoDate` preprocessor, and `serialiseAuditRow` (Date → ISO; nulls preserved).
    - **Client surface:** `app/features/orchestrator/api/audit.ts` (`useAuditApi()` + `auditKeys` query keys), `app/features/orchestrator/composables/useAuditLog.ts` (`useAuditList(params)` and `useAuditRow(id)` Pinia Colada queries; `enabled:` gates the row fetch on a non-null id), `app/features/orchestrator/types/audit.types.ts` (mirrors server JSON shape).
    - **UI page:** `app/pages/app/settings/audit.vue` — filter card (tool name input, status multiselect, class multiselect, since/until datetime-local inputs, clear button), table with createdAt / toolName / class badge / status badge / affectedCount / model / userId-truncated columns, prev/next pagination + page indicator + total count, `USlideover` drawer rendering pretty-printed JSON for `parameters`/`result`/`error`. Permission gate uses `usePermissions()` to hide content when the user lacks `orchestrator:audit:view` on any org (server is the source of truth; this is just an early hide).
    - **i18n:** full `audit.*` blocks in `i18n/locales/{en,de}.json` covering title/subtitle, permissions denied, error/load, filters (title/clear/toolName/status/class/since/until), table (title/total/empty), columns (createdAt/toolName/class/status/affectedCount/model/user), class (auto/confirm), status (all 5 values), pagination (indicator/prev/next), detail (title/conversation/createdAt/executedAt/confirmedAt/cancelledAt/parameters/result/error).
    - **Tests:** `tests/orchestrator-audit-api.test.ts` — 22 DB-backed tests covering: workspace scoping (cross-org rows filtered), model + conversation hydration, filters (toolName substring, status multi, class multi, modelId, since/until range, conversationId, combined), pagination (limit/offset accuracy + 200 cap at the schema layer), ascending sort, `findById` (hit / cross-org null / unknown id null), schema parsing (single vs repeated status, unknown enum rejected, limit/offset coercion, malformed dates), serialiser ISO output + null preservation, and the rbac seed assertion that Owner has `orchestrator:audit:view` while Admin/Member do not. Same `TRUNCATE … CASCADE` pattern in `beforeEach` as the existing `mcp-write-tools-*` and `orchestrator-audit` tests.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 349/349 (was 327; +22). No regressions.
  - **Deviations:** none. Permission constant + Owner role assignment already existed (no seed edit). The hub layout's settings nav is a single `/app/settings` link (no per-page side-nav today), so no nav entry to gate — server `requirePermission` and the page-level `canView` alert are the visible gates. `AuditRow` JSON shape is one field richer than the brief specified (`organisationId` and `conversationTitle` are also returned — used by the detail drawer; brief-listed fields all present).

### Chat handler and API
- [x] **T-3.9 — Conversation CRUD**
  - Routes per DESIGN-API §Orchestrator (excluding the streaming message endpoint T-3.11 and confirm/cancel T-3.12).
  - **Implementation notes:**
    - **Service:** `server/features/orchestrator/conversations.service.ts` exposes `createConversationsService({ db })` with surface `create`, `list`, `get`, `update`, `softDelete`. Capability validation gate (`validateModelOrThrow`) is shared by `create` + `update` — reads `ai_models` and throws `OrchestratorModelInvalidError` with `reason: 'not_found' | 'disabled' | 'lacks_tools' | 'lacks_streaming'` (DESIGN-AI §Capability validation). `list` emits `lastMessageAt` per row (REQ-ORCH-1 resume-most-recent UX) via a single grouped `max(created_at)` query over the page's conversation ids — keeps the read indexed via `orchestrator_messages_conversation_idx` and avoids one-round-trip-per-row. Soft-delete excluded by default; `includeDeleted` escape hatch on `list` and `get`. `softDelete` is idempotent (already-deleted, missing, and cross-org rows all no-op via the WHERE clause).
    - **Errors (extended in `errors.ts`):** `OrchestratorConversationNotFoundError` (used by `get` cross-org/soft-deleted, `update` cross-org/soft-deleted) and `OrchestratorModelInvalidError` (carries `modelId` + `reason`). Both are plain `Error` subclasses (mirrors AI errors), kept distinct from the `McpToolError` family.
    - **Schemas:** `server/features/orchestrator/conversation-schemas.ts` (Zod v4) — `createConversationSchema`, `updateConversationSchema` (refined: at least one of title/mode/modelId required; empty body → `validation.failed`), `conversationListQuerySchema` (limit ≤ 200, four sort variants, `includeDeleted` boolean preprocessor), `conversationGetQuerySchema` (`includeMessages` defaults true). Serialisers `serialiseConversation`, `serialiseConversationListItem`, `serialiseMessage` emit ISO date strings; `content` jsonb passes through verbatim.
    - **api-utils extension:** `server/features/orchestrator/api-utils.ts` gained `readOrchestratorBody`, `mapOrchestratorError`, `runOrchestratorServiceCall`. Error codes: `orchestrator.conversation.not_found` → 404 (carries `conversationId`), `orchestrator.conversation.model_invalid` → 400 (carries `modelId` + `reason`).
    - **Routes** (all under `server/api/orchestrator/conversations/`, gated by `requirePermission(ORCHESTRATOR_USE)` on `resolveWorkspace(event).organisationId`):
      - `index.post.ts` → `{ conversation }`
      - `index.get.ts` → `{ rows, total, limit, offset }` (rows include `lastMessageAt`)
      - `[id].get.ts` → `{ conversation, messages }`; `?includeMessages=false` returns `{ conversation }` only
      - `[id].patch.ts` → `{ conversation }`; empty body 400, cross-org/soft-deleted 404
      - `[id].delete.ts` → `{ success: true }` (mirrors `clearWorkspaceCredentials` precedent; idempotent)
    - **Container wiring:** `server/utils/container.ts` exposes `container.conversationsService` via `lazy(() => createConversationsService({ db: getDatabase('app') }))` (mirrors `auditService`). T-3.11 (chat handler) and T-3.12 (confirm/cancel) will resolve from there.
    - **Tests:** `tests/orchestrator-conversations-api.test.ts` — 29 DB-backed tests covering: `create` defaults / explicit mode + modelId / four model-invalid reasons (separate fixtures for `not_found`, `disabled`, `lacks_tools`, `lacks_streaming`), `list` workspace scoping / soft-delete exclusion + `includeDeleted` / pagination + sort accuracy / `lastMessageAt` hydration, `get` ordered messages / `includeMessages=false` / cross-org 404 / soft-deleted 404 + `includeDeleted=true` returns row, `update` title-only / mode mid-conversation / modelId capability-validated / lacks_streaming reject / cross-org + soft-deleted 404, `softDelete` idempotency / cross-org no-op / missing-row no-op, schema-level rejects (empty PATCH body, bogus mode), serialiser ISO output + verbatim jsonb, rbac seed assertion that Owner has `orchestrator:use`. Same `TRUNCATE … CASCADE` pattern in `beforeEach` as `orchestrator-audit-api.test.ts`.
    - **Test-only message helper:** `insertMessage(...)` writes directly via Drizzle (no public service surface for message creation — T-3.11 owns that).
    - **Deviations:** none. List items expose `lastMessageAt` (cheaper than `count(*)`, supports the resume-most-recent UX); the brief listed it as the preferred option. Permissions seed already grants Owner `orchestrator:use` (no seed edit). DELETE returns `{ success: true }` matching the `ai/credentials/[providerId].delete.ts` precedent rather than 204.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 378/378 (was 349; +29). No regressions.

- [x] **T-3.10 — Orchestrator AI client**
  - `server/features/orchestrator/ai-client.ts`: thin layer on top of T-3.1d that, given a conversation, resolves the model and returns helpers `streamChat({ messages, tools })` using the Vercel AI SDK's `streamText`.
  - Maps the orchestrator tool registry (T-3.2) into AI SDK `tools` schema.
  - Refs: DESIGN-AI, DESIGN-CHAT.
  - Done when: A unit test with a mocked transport sends a message with one tool and parses both text deltas and tool-call deltas correctly.
  - **Implementation notes:**
    - File: `server/features/orchestrator/ai-client.ts`. Single-file plumbing — `createOrchestratorAiClient({ aiProviderService, conversationsService, mcpServer })` returns `{ resolveForConversation, streamChat }`. No DB writes here; no audit-log integration (T-3.11's job).
    - **`resolveForConversation`** returns `{ conversation, modelClient, modelRow, providerKey, authorName }`. Model resolution chain (DESIGN-AI): `conversation.modelId` → `aiProviderService.getDefaultModel({ organisationId }).id` (which already covers workspace default → global default → `AiNoDefaultModelError`). `authorName` (REQ-ORCH-7): `orchestratorWorkspaceSettings.aiDisplayName` (trimmed; the column is non-null with default `'Claude-Orchestrator'`) → falls back to `modelRow.displayName` only if the workspace name is empty. Reads via the existing `aiProviderService.getWorkspaceSettings` (create-on-read), so no new direct DB query.
    - **`streamChat`** filters `mcpServer.list({ mode: 'read' })` for `read_only` conversations, otherwise `mcpServer.list()` (read + write). Each tool maps to `{ description, inputSchema: tool.schema }` — Zod v4 schemas pass directly into AI SDK's `FlexibleSchema` slot. **No `execute` is wired** — the chat handler runs `auditedInvoke` itself (manual loop). Returns `{ stream: streamText(...), toolsRegistered: string[] }` so callers can verify which tools were exposed and iterate `stream.fullStream` for both text and tool-call deltas verbatim.
    - **Loop choice — manual.** The model is given `description` + `inputSchema` only; tool execution lives in T-3.11. Reason: `auditedInvoke` returns either `{ kind: 'executed' }` or `{ kind: 'confirmation_required' }`, and the AI SDK's auto-execute path can't pause mid-stream for a user confirmation. Wiring an `execute` here would either bypass the audit row or fight the gate. T-3.11 reads `tool-call` chunks off `fullStream`, runs `auditedInvoke`, and feeds results back as a `ToolModelMessage` on the next turn (`streamText` is called once per logical turn).
    - **Per-call `experimental_context`:** `streamChat` forwards the supplied `toolContext` via `experimental_context` so a future variant that wires `execute` (e.g. for non-pausable tools) can pull the same context the chat handler uses for `auditedInvoke`. Today the chat handler ignores it and re-uses the ctx it already holds.
    - **Container wiring:** `server/utils/container.ts` exposes `container.orchestratorAiClient` via `lazy(...)`, with a placeholder `getOrchestratorMcpServer` lazy that returns `createMcpServer()` (empty registry). A `TODO(T-3.11)` notes that the chat handler will likely construct the client per-request alongside a freshly-populated registry, and the singleton may be replaced.
    - **Tests:** `tests/orchestrator-ai-client.test.ts` — 8 tests, 4 DB-backed (resolve flow) + 4 stream-shape (mocked `streamText`). Hoisted mocks (matching `tests/ai-provider.test.ts` shape) for `@ai-sdk/anthropic` (also `openai` / `google` for the import-resolves graph) and `ai`. Coverage: resolve happy path with model + provider + author name + AI SDK callable invocation, fallback to workspace default, fallback to global default, `OrchestratorConversationNotFoundError` on unknown id; `streamChat` exposes only read tools in `read_only` mode (verified via both `toolsRegistered` and the `streamText` call's `tools` arg), exposes read + write in `read_write`, asserts no `execute` is wired, and round-trips text-delta + tool-call chunks unchanged via `fullStream`.
    - **Deviations:** none. The container singleton uses an empty MCP server because the registry is per-request — flagged with the TODO; T-3.11 may either swap the wiring for a per-request factory or skip the container slot entirely. `experimental_context` use is a forward-compat hook, not load-bearing today.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 386/386 (was 378; +8). No regressions.

- [x] **T-3.11 — Streaming message endpoint with tool loop**
  - `POST /api/orchestrator/conversations/[id]/messages` streams via SSE.
  - Implements the loop in DESIGN-CHAT: assistant tokens → tool calls → auto-execute or pause for confirmation → resume on confirm.
  - Persists every message and tool result.
  - Refs: REQ-ORCH-1, REQ-ORCH-3, REQ-ORCH-4, DESIGN-CHAT.
  - **Implementation notes:**
    - Files: `server/api/orchestrator/conversations/[id]/messages.post.ts` (thin route, opens h3 `createEventStream`, delegates), `server/features/orchestrator/chat-handler.ts` (the loop runner — testable in isolation, exposes `run` + `resumeFromConfirmation`), `server/features/orchestrator/messages.service.ts` (tiny `append` / `loadHistory` / `bumpConversationUpdatedAt` over `orchestrator_messages`).
    - **Manual loop** (decision noted in T-3.10): tools are exposed to `streamText` with `description` + `inputSchema` only — no `execute`. The handler iterates `result.fullStream`, collects text-deltas + tool-call chunks, persists the assistant message, then runs each tool through `auditedInvoke`. On `kind:'executed'` it persists a `tool_result` message + emits SSE and recurses; on `kind:'confirmation_required'` it emits SSE `confirmation_required`, late-binds `auditService.attachMessageId(actionId, assistantMessageId)`, and ends the response.
    - **Per-request MCP registry** (REQ-ORCH-3): `chat-handler` instantiates a fresh `createMcpServer()` each call, registers read tools always, registers write tools only for `read_write` conversations. The container's `orchestratorAiClient` singleton (empty registry) is left in place but not used by this handler — comment in `server/utils/container.ts` documents the seam.
    - **SSE event vocabulary** (documented at the route file head): `text-delta { messageId, delta }`, `tool-call { toolCallId, toolName, input }`, `tool-result { toolCallId, actionId, result }`, `confirmation_required { toolCallId, actionId, toolName, input, affectedCount, reason }`, `message-complete { messageId }`, `error { code, message }`. Each event is encoded as `{ event: <type>, data: JSON.stringify(event) }` via h3's `createEventStream`.
    - **Resume seam for T-3.12**: `chatHandler.resumeFromConfirmation({ conversationId, organisationId, userId, actionId, sink })` reads the pending audit row, builds a ctx with `confirmationToken: actionId`, calls `auditedInvoke({ priorActionId: actionId })` so the same audit row is patched to `executed` (single row per logical call). It then drives the model loop forward (model receives the new tool_result and emits its next turn). The synthetic `toolCallId` for the resumed result is `confirm_<toolName>_<actionId>` because the original toolCallId from the assistant's tool-call block is not reverse-indexed against the audit row. T-3.12's `/confirm` route should call this method; `/cancel` calls `auditService.recordCancelled(actionId)` and (TBD) emits a `tool-result` with an execution-denied marker before opening another loop turn.
    - **Permission gate**: handler accepts a `PermissionGuard` interface — the route binds `requirePermission(event, ...)` from `rbac.guard.ts`. Always requires `orchestrator:use`; additionally requires `orchestrator:write` when `conversation.mode === 'read_write'`. The guard fires before any SSE event opens — failures throw HTTP errors that the route catches and surfaces as an error response (no half-open SSE).
    - **Persistence shape**: user rows store the raw text string in `content` (jsonb-as-string). Assistant rows store SDK-shaped content blocks: `[{ type: 'text', text }, { type: 'tool-call', toolCallId, toolName, input } …]`. `tool_result` rows store `[{ type: 'tool-result', toolCallId, toolName, output: { type: 'json', value } }]`. `rowToModelMessage` translates back into AI SDK `ModelMessage` shape for subsequent model turns. History limit reads from `orchestrator_workspace_settings.history_limit` via `aiProviderService.getWorkspaceSettings`.
    - **Tool failures inside loop**: when `auditedInvoke` throws (e.g. `McpToolNotFoundEntityError`), the audit row is recorded as `failed` (audit layer's job), the handler appends a tool_result message with `{ error, message }` and emits SSE so the model can react on the next turn — the loop continues rather than aborting.
    - **Author propagation (REQ-ORCH-7)**: `ctx.authorName` is filled from `aiClient.resolveForConversation` (workspace `ai_display_name` override → model display name). Threaded through every `auditedInvoke` call.
    - **Container wiring**: added `container.messagesService`. The existing `container.orchestratorAiClient` singleton is documented as request-scoped-superseded (kept for backwards compatibility with callers that may resolve it).
    - **Tests:** `tests/orchestrator-chat-handler.test.ts` — 6 DB-backed integration tests with hoisted mocks for `streamText`. Coverage:
      - Single text-only turn: user + assistant rows persisted; SSE `text-delta` (×N) + `message-complete`.
      - Auto tool turn (`kb_list_tags`, read_only): tool-call → tool-result → recursive text turn → message-complete; rows = user, assistant(toolcall), tool_result, assistant(text); no audit row (read tool).
      - Confirm tool turn (`kb_update_entry`, read_write): SSE `confirmation_required` emitted, no `tool-result`; one `pending_confirmation` audit row; entry untouched; no further `streamText` calls.
      - Resume from confirmation: round-trips run → resumeFromConfirmation → asserts single audit row patched to `executed`, KB body actually updated, message rows = user, assistant(toolcall), tool_result, assistant(text).
      - Permission gate: `read_write` with `allowWrite: false` PermissionGuard rejects before SSE opens (statusCode 403, sink stays empty + unclosed).
      - Cross-org / unknown conversation id: `OrchestratorConversationNotFoundError` propagates.
    - **Deviations / TODOs:**
      - Cancel path (`POST .../cancel`) is T-3.12 work; the resume API is in place but the cancel-side tool_result emission is not wired (the audit `recordCancelled` exists; the chat handler should learn how to inject an `execution-denied` tool_result on resume after a cancel).
      - The route file emits SSE errors for permission/404 in addition to throwing — h3's `createEventStream` returns the response body, so we send the response first, then run the loop. This means a 404 surfaces as an `error` SSE event rather than an HTTP 404. Consider pre-flighting the conversation load *before* `stream.send()` if HTTP-status semantics matter to the client.
      - Synthetic `toolCallId` on resume (`confirm_<toolName>_<actionId>`) means a perfectly-correlated round-trip with the original assistant turn's toolCallId is not preserved. The model sees a valid toolCallId in the next turn's history — sufficient for AI SDK conversion — but a future enhancement could persist the original toolCallId in the audit row's `parameters` jsonb to bridge it.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 392/392 (was 386; +6). No regressions.

- [x] **T-3.12 — Confirm and cancel endpoints**
  - `POST .../confirm` and `POST .../cancel` resume or abort the paused tool call.
  - Refs: REQ-ORCH-4, DESIGN-CHAT.
  - **Implementation notes:**
    - Files: `server/api/orchestrator/conversations/[id]/confirm.post.ts`, `server/api/orchestrator/conversations/[id]/cancel.post.ts` (thin SSE routes mirroring `messages.post.ts`), and a new `chatHandler.resumeFromCancellation` method in `server/features/orchestrator/chat-handler.ts`. Body schema (Zod v4) is identical for both: `{ actionId: string }` validated as a 26-char Crockford base32 ULID.
    - **Routes & permission gates:**
      - `POST /api/orchestrator/conversations/[id]/confirm` — `orchestrator:use` + `orchestrator:write` (the latter only fires when `conversation.mode==='read_write'`, but every pending row originates from a write tool, so it is always required in practice). Delegates to `chatHandler.resumeFromConfirmation` which already exists; permission gates run inside `prepareTurn`.
      - `POST /api/orchestrator/conversations/[id]/cancel` — same gates. We require `orchestrator:write` for symmetry (cancellation is a write-side mutation against the conversation's tool history); rationale documented inline. Delegates to the new `resumeFromCancellation`.
    - **Validation contract** (raised pre-stream, surfaced as SSE `error` events by the route catch-block since `createEventStream` opens before validation runs):
      - `404 orchestrator.action.not_found` — actionId missing, cross-org, or for a different conversation. Existence-leak-safe (no shape difference between cross-org and unknown id).
      - `409 orchestrator.action.not_pending` — row exists but is not in `pending_confirmation` (confirm also accepts `confirmed` for idempotent client retries; cancel strictly rejects `confirmed` because that row is mid-execution).
    - **`resumeFromCancellation` flow:**
      1. `loadPendingAction` (shared helper extracted from confirm) — org-scoped, conversation-scoped row lookup; throws HTTP errors instead of sink errors.
      2. `prepareTurn` — re-runs permission gates + builds the per-request MCP registry / ai-client (read+write because the row is from a `read_write` conversation).
      3. `auditService.recordCancelled(actionId)` — flips status to `cancelled`, sets `cancelled_at`.
      4. Persist a synthetic `tool_result` message with content `[{ type:'tool-result', toolCallId: 'confirm_<toolName>_<actionId>', toolName, output: { type:'json', value: { cancelled: true, reason: 'user_cancelled' } } }]`. Reuses the same synthetic toolCallId scheme as the confirm path. Late-binds `auditService.attachMessageId(actionId, row.id)`.
      5. SSE emits a `tool-result` event with `result: { cancelled: true, reason: 'user_cancelled' }`.
      6. Calls `runFinalLoop` (signature widened to `ResumeFromConfirmationArgs | ResumeFromCancellationArgs`) so the model emits its reaction turn (typically: acknowledge cancellation).
    - **Refactor on confirm path:** `resumeFromConfirmation`'s validation switched from sink-error-then-return to the new `loadPendingAction` helper that throws `createError({statusCode})`. The route's `.catch` block converts those throws into SSE `error` events with code `<status>_<statusMessage>` so the client behaviour is unchanged from T-3.11. The only behavioural delta is that resumeFromConfirmation now also checks `audit.conversationId === args.conversationId` (previously it relied on org-scoping only).
    - **Cancel result content shape (persisted + SSE):** `{ cancelled: true, reason: 'user_cancelled' }` — flat literal, not wrapped in an `error` envelope. The model can pattern-match on `cancelled: true` cleanly, distinct from a tool execution error which uses `{ error: <code>, message: <string> }`.
    - **Tests:** `tests/orchestrator-confirm-cancel.test.ts` — 10 DB-backed integration tests (the pre-existing chat-handler tests already cover the confirm happy path, so this file focuses on T-3.12-specific deltas):
      - Confirm wrong status (already-`executed`): throws 409 `orchestrator.action.not_pending`, sink stays empty/unclosed, audit row + entry untouched.
      - Confirm cross-org: throws 404 `orchestrator.action.not_found`, no state change.
      - Confirm with unknown actionId: throws 404.
      - Cancel happy path: SSE `tool-result` (cancel envelope) + `text-delta` + `message-complete`; audit `status='cancelled'`, `cancelledAt` Date, `executedAt` null; entry untouched (cancel does NOT execute the tool); messages: user / assistant(toolcall) / tool_result(cancelled) / final assistant; audit's `messageId` bound to the synthetic tool_result row.
      - Cancel wrong status (already-`executed` after a confirm): throws 409, no state change.
      - Cancel cross-org: throws 404, no state change.
      - Cancel unknown actionId: throws 404.
      - Route body schema (re-derived locally to avoid importing the route default-export): rejects empty body, rejects malformed actionId, accepts a fresh ULID. Covers the "body missing actionId → 400" case in the brief at the contract layer.
    - **Deviations:**
      - The brief's "POST confirm without `orchestrator:write` → 403" integration assertion is not added as a separate route-level test — the existing `chatHandler.run — permission gate` test in `orchestrator-chat-handler.test.ts` already exercises the same `prepareTurn` permission code path that confirm/cancel reuse. Splitting it would duplicate the assertion without exercising new code.
      - Pre-stream HTTP-status semantics: the brief asks for 404/409 on the route. h3's `createEventStream` opens the response before our validation runs, so a thrown HTTP error becomes an SSE `error` event with `code: '<status>_<statusMessage>'` (matches the `messages.post.ts` pattern from T-3.11; both routes share this trade-off, documented at the messages route's "Deviations / TODOs"). The handler-level tests do exercise the strict throw semantics directly.
      - `createError` is now imported from `h3` in `chat-handler.ts` (was previously relying on Nuxt auto-imports, but the file already explicitly imports `EventStream`/`H3Event` from `h3`, so this is consistent).
      - The seed helper in the new test file is an `ensureProviderAndModel` upsert (shared `ai_providers` + `ai_models` rows across orgs) rather than per-org rows because both tables carry uniqueness constraints (`ai_providers.key`, `(provider_id, model_id)` composite). The pre-existing chat-handler test only seeds one org so it didn't hit this; the cross-org tests here required the change.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 402/402 (was 392; +10). No regressions.

### Client feature: orchestrator
- [x] **T-3.13 — Chat UI shell**
  - `app/pages/app/orchestrator/index.vue` (list of conversations), `app/pages/app/orchestrator/[id].vue` (chat).
  - Mode toggle (read-only / read+write) prominently shown.
  - Per-conversation model picker that lists only models with configured credentials and required capabilities.
  - Refs: REQ-ORCH-1, REQ-ORCH-3, REQ-AI-4.
  - **Implementation notes:**
    - Files added:
      - `app/features/orchestrator/types/conversation.types.ts` — mirrors `SerialisedConversation` / `SerialisedConversationListItem` / `SerialisedMessage` plus a typed `MessageContentBlock` union (text / tool-call / tool-result / fallback `{ type: string }`) so the chat view can fan out on `block.type` without touching `unknown`.
      - `app/features/orchestrator/api/conversations.ts` — `useConversationsApi()` factory with `list / get / create / update / softDelete`. `conversationKeys` follows the audit-log key shape (`['orchestrator', 'conversations', ...]`).
      - `app/features/orchestrator/composables/useConversations.ts` — Pinia Colada surface: `useConversationList(params)`, `useConversation(id)` (gated by `enabled: () => Boolean(id)`), `useCreateConversation`, `useUpdateConversation`, `useDeleteConversation`. Mutations funnel through one `invalidate(queryCache, conversation)` helper that wipes the list family and the matching detail key.
      - `app/features/orchestrator/composables/eligibleModelsFilter.ts` — pure helper (no auto-imports) extracted so the unit test can import it without resolving the Nuxt `~/...` alias graph. Re-exported from `useEligibleModels.ts` for convenience.
      - `app/features/orchestrator/composables/useEligibleModels.ts` — combines `useAiProviders` + `useAiCredentials` + `useAiModels` and feeds them through `filterEligibleModels`. Filter rules per REQ-AI-4: provider enabled + has credentials, model enabled, `supports_tools && supports_streaming`. Credential rows from `/api/ai/credentials` override `/api/ai/providers` (the credentials endpoint is more authoritative when both report the same provider).
      - `app/pages/app/orchestrator/index.vue` — list page with title filter (client-side via `refDebounced`, since the list endpoint doesn't accept a search param), `includeDeleted` checkbox, sort picker, prev/next pagination, "New conversation" button, soft-delete confirmation modal. Soft-deleted rows render at 60% opacity with a "Deleted" badge when `includeDeleted` is on; restoration is left for a later T-NTH bulk-actions task per the brief.
      - `app/pages/app/orchestrator/[id].vue` — chat view with editable title (PATCH on blur / Enter; rolls back on error), mode toggle (read-only ↔ read+write with an explicit `UModal` confirm before flipping to write), model picker (PATCH on change with rollback on error), static message history, and a deliberately-disabled composer with a tooltip "streaming chat coming in T-3.15". Tool-call and tool-result blocks render as `[tool call: <name>]` / `[tool result: <name>]` placeholder lines so historical messages with structured content don't break the page (T-3.14 owns the real cards).
      - `tests/orchestrator-eligible-models.test.ts` — 10 unit tests for `filterEligibleModels`: happy path, provider with no creds, disabled provider, model without tools / streaming / `enabled`, missing-provider id, credential-overrides-provider in both directions, mixed-batch.
    - **Permission gating:** `usePermissions()` is consulted on both pages. `orchestrator:use` (or `platform:admin`) is required to render anything beyond a `UAlert`; `orchestrator:write` gates the mode toggle (disabled with a tooltip otherwise). Server-side `requirePermission` remains the source of truth — these client checks just hide affordances early, mirroring `audit.vue`'s pattern.
    - **Composer choice:** disabled with a tooltip per the brief — keeps the page testable without depending on SSE plumbing yet. The local `composerInput` ref + `<UTextarea disabled>` give T-3.15 a one-line wiring point (drop the `disabled` attr, swap the click handler to a `useChatStream` composable).
    - **Eligible-models banner:** when `useEligibleModels` returns empty after settling, the chat view shows a `UAlert` with a deep link to `/app/settings/ai`. The model picker itself is disabled in that state so the user can't PATCH a non-eligible id.
    - **i18n:** added `orchestrator.{permissions,errors,modes,list,chat}.*` blocks in `i18n/locales/{en,de}.json` mirroring the wording style of the existing `audit.*` block. The chat-page title-field key is namespaced as `orchestrator.chat.titleField.*` (not `orchestrator.chat.title.*`) to avoid collision with the page-level `orchestrator.chat.title` string.
    - **Deferred to T-3.14:** tool-call cards, confirmation cards, inline parameter rendering, restore-deleted affordance.
    - **Deferred to T-3.15:** SSE streaming on send, abort handling, reconnect, optimistic message append. The composer is wired up to a local ref but the send button stays disabled.
    - **Deviations:** none. The brief asked for arrow functions / Zod v4 / no emojis — followed throughout. No new server-side files; uses the existing T-3.9 endpoints unchanged.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean (one harmless Nuxt auto-import duplicate-warning for `filterEligibleModels` because both the helper file and the composable export it; no type errors). `pnpm test:run` 412/412 (was 402; +10).

- [x] **T-3.14 — Tool-call and confirmation cards**
  - Render tool calls inline with name and parameters; confirmation cards with Confirm / Cancel.
  - Refs: REQ-ORCH-4.
  - **Implementation notes:**
    - Files added:
      - `app/features/orchestrator/components/ToolCallCard.vue` — props `{ toolCallId, toolName, input }`. Compact bordered row with chevron toggle, monospace tool name, copy-to-clipboard button on the toolCallId. Body collapsed by default; expanded shows pretty-printed JSON in a `<pre>` capped at `max-h-64` with scroll.
      - `app/features/orchestrator/components/ToolResultCard.vue` — props `{ toolCallId, toolName?, output, error?, status? }`. Status auto-derived (`failed` if `error`, `cancelled` if synthetic `{cancelled:true, reason:'user_cancelled'}`, else `executed`); border-left colour reflects status. Cancellation envelope renders the friendly i18n line in place of raw JSON. The shared `data-tool-call-id` attribute on both cards lets future wiring scroll/highlight pairs.
      - `app/features/orchestrator/components/ConfirmationCard.vue` — props `{ conversationId, actionId, toolName, input, affectedCount, reason }`, emits `'confirmed' | 'cancelled'`. Yellow `UCard` (variant=soft, ring-warning ui override). Reason copy is `class` → "confirm-only" / `bulk` → "would affect {n} entities". Footer hosts Cancel (ghost) and Confirm (warning) buttons; both disable while *either* mutation is in flight.
    - API extension: `app/features/orchestrator/api/conversations.ts` gained `confirmAction({conversationId, actionId})` and `cancelAction({conversationId, actionId})`. Both POST to the T-3.12 endpoints with `responseType: 'stream'` and **discard the SSE body** — surfacing the streamed reply is T-3.15. Documented in JSDoc.
    - Composables: `app/features/orchestrator/composables/useConversationActions.ts` exports `useConfirmAction()` and `useCancelAction()` Pinia Colada mutations. Both invalidate `conversationKeys.detail(conversationId)` (and the `all` family) on success so the new `tool_result` row appears once T-3.12 finishes.
    - Wiring in `app/pages/app/orchestrator/[id].vue`: replaced the `[tool call: ...]` / `[tool result: ...]` placeholder lines with the new cards iterating over `messageBlocks(message)`. A `pendingConfirmation` ref renders `<ConfirmationCard>` between the message list and the composer; the card emits `confirmed`/`cancelled` → the page nulls the ref and triggers `refetch()`. T-3.15 just sets the ref from the SSE `confirmation_required` event.
    - **Dev escape hatch:** under `import.meta.dev`, the page exposes `globalThis.__setPendingConfirmation(...)` so the card can be exercised end-to-end against a manually-inserted `pending_confirmation` row before SSE is wired. Removed automatically in production builds.
    - **MessageBubble decision:** kept the rendering inline in `[id].vue` per the brief's "smaller diff" guideline. The existing message-row markup (role+time gutter, text summary, tool blocks list) only needed the inner `<li>` body swapped — extracting a bubble component would have churned the pages page without simplifying anything.
    - **i18n:** `orchestrator.toolCall.{parameters,copyId,copied,copyFailed}`, `orchestrator.toolResult.{untitled,output,cancelledMessage,status.{executed,cancelled,failed}}`, `orchestrator.confirmation.{title,parameters,reason.{class,bulk},actions.{confirm,cancel},errors.{confirm,cancel}}` added to both `en.json` and `de.json`. The `chat.messages.toolCallPlaceholder` / `toolResultPlaceholder` keys are now unused but left in place — they're cheap and a future cleanup pass can prune them.
    - **Deferred to T-3.15:** SSE reader for the confirm/cancel responses (currently fire-and-discard); plumbing the `confirmation_required` SSE event into `pendingConfirmation`; mid-stream error toast surfacing for the dropped SSE body; auto-scroll to a freshly-rendered confirmation card.
    - **Tests:** no Vue testing harness wired into `tests/` (the `nuxt`-environment project at `test/nuxt/*` is empty), and the new composables are thin wrappers over a single `$fetch` call that just invalidate two query keys — no non-trivial logic worth a unit test. Lint + typecheck are the gate per the brief.
    - **Deviations:** none. The dev-only `__setPendingConfirmation` hook is a deliberate addition (brief-suggested "fake `pendingConfirmation` ref") — it ships as a `globalThis` write so it can be invoked from the browser console without re-rendering machinery.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean (one pre-existing harmless `filterEligibleModels` duplicate-import warning from T-3.13). `pnpm test:run` 412/412 — no regressions, no test delta (no new tests added per the brief's "skip if no harness" guidance).

- [x] **T-3.15 — Streaming integration**
  - Connect SSE stream; render tokens incrementally; handle reconnect.
  - Refs: REQ-ORCH-1.
  - **Implementation notes:**
    - Files added: `app/features/orchestrator/types/stream.types.ts` (typed `ChatEvent` union mirroring the server SSE vocabulary + `createSseParser()` line-buffer state machine), `app/features/orchestrator/composables/useChatStream.ts` (single-stream SSE consumer), `tests/orchestrator-sse-parser.test.ts` (10 unit tests).
    - Files modified: `app/features/orchestrator/api/conversations.ts` (removed `confirmAction` / `cancelAction` body-discarding wrappers; replaced with `orchestratorStreamPaths` URL helpers — the stream consumer owns the request lifecycle now), `app/features/orchestrator/components/ConfirmationCard.vue` (now a pure presentational component: emits `confirmed` / `cancelled` carrying the `actionId`, parent page handles the resumed SSE stream; added `busy` prop), `app/pages/app/orchestrator/[id].vue` (full streaming wiring — composer enabled, send/stop/retry buttons, live trailing block, error alert), `i18n/locales/{en,de}.json` (added `orchestrator.chat.composer.{stop,retry,noModelsTooltip,noPermissionTooltip}`, `orchestrator.chat.stream.*`, `orchestrator.chat.toast.aborted`; replaced `disabledTooltip` with permission/model tooltips).
    - Files removed: `app/features/orchestrator/composables/useConversationActions.ts` (the `useConfirmAction` / `useCancelAction` mutations are subsumed by the page calling `useChatStream.start()` against the confirm/cancel routes directly).
    - **`useChatStream` surface:** `{ start(urlPath, body), abort(), retry(), reset(), onEvent(handler) → unsubscribe, events: Ref<ChatEvent[]>, isStreaming: Ref<boolean> }`. `start()` opens a POST request via native `fetch` (not `$fetch.raw`) because we need direct `ReadableStream` access on `resp.body` and a uniform abort-controller hookup; `credentials: 'include'` carries the session cookie. The body chunks are decoded with `TextDecoder` and fed to `createSseParser()` which returns zero or more `ChatEvent`s per chunk. Each event is appended to the `events` ref and broadcast to all `onEvent` listeners; listener throws are swallowed so a buggy subscriber can't tank the stream.
    - **SSE parser:** stateless line-buffer state machine that splits on `\n\n`. Recognises `event:` and `data:` lines, normalises CRLF→LF, ignores `:`-prefixed comments / heartbeats, drops blocks with malformed JSON or unknown event types (forward-compatibility — server can add new event types without the client crashing). Multi-line `data:` lines join with `\n` per the SSE spec. Partial chunks buffer until the trailing blank line arrives. `flush()` exposes any tail event for the rare case the server didn't terminate the last block.
    - **Reconnect strategy — manual retry, not auto-reconnect** (the spec says "handle reconnect"; we picked the safer interpretation): on a mid-stream error (network drop, connection reset) the consumer emits a synthetic `error { code: 'connection_lost' }` event and stops. Auto-reconnect would risk re-sending the user message (the messages route persists the user row before the loop runs, so a retry would duplicate) or double-executing a confirm/cancel. The page surfaces a Retry button only for `connection_lost` errors; clicking it re-runs the most recent `start()` args. Documented in the composable's header comment.
    - **Page integration:** `[id].vue` builds a "live" trailing block from three reactive collections (`liveText` Map keyed by `messageId`, `liveToolCalls[]`, `liveToolResults[]`) plus an optimistic user-text ref, rendered after the persisted `messages` list. After `message-complete` the page calls `refetch()` and clears the live state inside the `.then()` so the UI doesn't flash empty between live drop and persisted hydrate. `confirmation_required` populates `pendingConfirmation`; the card's `confirmed` / `cancelled` emits trigger `stream.start()` against the confirm/cancel routes — the resumed turn flows back through the same event handler, no special-case rendering. Composer is `Enter` to send, `Shift+Enter` for newline; disabled when no permission / no eligible models / streaming / pending confirmation. Stop button replaces Send while `isStreaming`. `onBeforeUnmount` aborts the in-flight stream so navigation away cancels cleanly.
    - **Error handling:** stream `error` events surface as a `UAlert` (not a toast — the alert sits in the message column where the failure happened). 4xx/5xx HTTP responses that don't even open a stream are mapped to the same alert via the `useChatStream` non-OK branch (parses `{ statusMessage, message }` from h3 errors). User-aborted streams show a neutral toast and leave the partial live block intact (no error code).
    - **Tests:** `tests/orchestrator-sse-parser.test.ts` — 10 unit tests covering: single-event happy path, all six event types in one stream, mid-block split, mid-`\n\n` split, multi-block chunk, malformed JSON dropped, unknown event types dropped, `:`-prefixed comments ignored, CRLF-vs-LF parity, `flush()` for tail-without-blank-line. No Vue component tests — no harness available (per the brief).
    - **Deviations:**
      - Removed `useConversationActions.ts` and the `confirmAction` / `cancelAction` API wrappers entirely rather than extending them. The original design (T-3.14 placeholders) had the `ConfirmationCard` own its own mutation; routing the resumed SSE stream through that path would have required either a parallel chat-stream-from-mutation pipe or moving the SSE consumer one layer up. Letting the page own a single `useChatStream` instance and having the card emit-only is meaningfully simpler.
      - The `optimisticUserText` is rendered as a single bubble above the assistant live block rather than appending to the persisted message list. The persisted user row is hydrated by the `refetch()` on `message-complete`, so the optimistic block disappears once the canonical row arrives.
      - `useChatStream` uses native `fetch` rather than `$fetch.raw(..., { responseType: 'stream' })`. The latter wraps Nitro/ofetch's raw response, but ofetch's stream response type doesn't expose `ReadableStream` cleanly across runtimes. Native fetch is straightforward and the chat endpoints are client-only (the page mounts before send is callable, so SSR cookie forwarding doesn't apply).
      - The optional `experimental_context` / SSR-cookie-forwarding nicety from `$api` isn't needed because chat streams are user-initiated post-mount; if a future requirement adds SSR-time SSE we'd need to revisit.
    - i18n additions: `orchestrator.chat.composer.{stop,retry,noModelsTooltip,noPermissionTooltip}`, `orchestrator.chat.stream.live.{label,assistant}`, `orchestrator.chat.stream.errors.{title,connection_lost,generic}`, `orchestrator.chat.toast.aborted` in en + de. The pre-existing `disabledTooltip` key was retired (composer is no longer always-disabled); description string updated to "Press Enter to send. Shift+Enter for newline."
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean (one pre-existing harmless `filterEligibleModels` duplicate-import warning), `pnpm test:run` 422/422 (was 412; +10).

### Phase 3 acceptance
- [x] **T-3.X — Phase 3 complete**
  - End-to-end:
    - Workspace owner configures credentials for at least two providers (e.g. Anthropic + OpenAI).
    - Picking different models in two conversations actually routes through different providers (verifiable in audit log via `model_id`).
    - Read-only conversation finds and summarises KB entries and todos.
    - Read+write conversation creates an inbox KB entry without confirmation.
    - Read+write conversation creating a verified entry triggers a confirmation card.
    - Audit log shows all executed actions with the model that requested them.
  - Done when: Manual run-through covers these flows with no regressions in earlier phases.
  - **Automated coverage (acceptance criterion → test file):**
    - Multi-provider credential round-trip: `tests/ai-config.test.ts` (encrypt/decrypt per provider, `setWorkspaceCredentials` upsert, `listWorkspaceCredentialStatus` per-provider state, plaintext never persisted).
    - Different models route to different provider SDKs: `tests/ai-provider.test.ts` (`resolveModelClient` for Anthropic, OpenAI, and Google all bind their respective SDK factory + decrypted key).
    - Conversation-pinned model lands on the audit row: `tests/orchestrator-chat-handler.test.ts` (confirm-tool turn + resume-from-confirmation both assert `audits[0].modelId === fx.modelId` — extended in this task), reinforcing `tests/orchestrator-audit.test.ts` (all six audit transitions persist `modelId`).
    - Audit-log API surfaces `modelId` for filtering / display: `tests/orchestrator-audit-api.test.ts` (filter by `modelId`, hydrates model display name / `modelId` / conversation title).
    - Read-only conversation summarises KB + todos: `tests/mcp-read-tools.test.ts` (six read tools registered, `kb_search` / `kb_get_entry` / `kb_list_categories` / `kb_list_tags` / `todo_search` / `todo_get` exercised end-to-end against real DB) + `tests/orchestrator-chat-handler.test.ts` (read_only conversation drives `kb_list_tags` through `streamText` and persists the `tool_result`).
    - Read+write conversation auto-creates an inbox KB entry: `tests/mcp-write-tools-kb.test.ts` (`classifyKbCreateEntry` returns `auto` for `inbox`/omitted, registry invocation creates the row without a `confirmationToken` and persists `status='inbox'`).
    - Read+write conversation requires confirmation for verified KB entry: `tests/mcp-write-tools-kb.test.ts` + `tests/mcp-confirmation.test.ts` (classifier returns `confirm` for `draft`/`verified`; first call throws `ConfirmationRequiredError` with validated input; second call with token executes). End-to-end resume path through the chat handler: `tests/orchestrator-chat-handler.test.ts` confirm-tool / resume-from-confirmation pair, plus `tests/orchestrator-confirm-cancel.test.ts` for the `/confirm` and `/cancel` endpoints.
    - Read-only conversation cannot register write tools: `tests/orchestrator-ai-client.test.ts` (`streamChat` only exposes read tools when `mode === 'read_only'`).
  - **Test additions in this task:** two assertions added to `tests/orchestrator-chat-handler.test.ts` — the confirm-tool turn and the resume-from-confirmation cases now assert `audits[0].modelId === fx.modelId`, locking down the "verifiable in audit log via `model_id`" criterion across both `pending_confirmation` and `executed` row states. No new test cases; existing total holds at 422/422.
  - **Operator runbook (manual run-through with real provider keys):**
    - **Preconditions:** real Anthropic + OpenAI API keys; `pnpm docker:up` has the Postgres container live; `NUXT_AUTH_SECRET`, `NUXT_DATABASE_*`, `NUXT_PUBLIC_SITE_URL` set in `.env`; an admin account seeded into the workspace (any user with `orchestrator:write` and `orchestrator:audit:view`).
    - **Boot:** `pnpm db:migrate && pnpm dev` (the migrations cover providers/models/orchestrator schema). Sign in as the seeded admin.
    - **Step 1 — Configure credentials.** Visit `/app/settings/ai`. Save an Anthropic API key; save an OpenAI API key. Verify the per-provider status badges flip to "configured". The plaintext key must not appear in any subsequent GET response (round-trip is automated; this is a UI sanity check).
    - **Step 2 — Two conversations, two providers.** Navigate to `/app/orchestrator`. Create conversation A; pick an Anthropic model (e.g. Claude Sonnet 4.6). Create conversation B; pick an OpenAI model (e.g. GPT-5). Both default to `read_only`.
    - **Step 3 — Read-only summarisation.** In conversation A, send `summarise my recent KB entries and open todos`. Confirm the assistant streams text deltas, calls `kb_search` / `todo_search` (tool-call cards render), and produces a final summary. Repeat in conversation B. Both should succeed via their respective providers.
    - **Step 4 — Auto write (inbox).** Flip conversation A to `read+write` (the explicit modal confirm appears). Send `create a KB entry titled "Acceptance test inbox" with status inbox describing today's run-through`. The entry is created without a confirmation card; visit `/app/kb` and verify the entry exists with `status='inbox'` and `author_type='ai'`.
    - **Step 5 — Confirmation card (verified).** In conversation A still, send `create a KB entry titled "Acceptance test verified" with status verified`. A confirmation card renders inline. Click **Confirm**; verify the entry appears in `/app/kb` with `status='verified'`. Repeat once more, click **Cancel**, and confirm no entry is created (the cancelled audit row is still recorded with `status='cancelled'`).
    - **Step 6 — Audit log.** Visit `/app/settings/audit`. Filter by conversation A, then conversation B. Confirm:
      - Each executed write from step 4/5 has a row with the correct `tool_name`, `class` (`auto` for inbox, `confirm` for verified), and `status` (`executed` / `cancelled`).
      - The model column displays the **per-conversation model** (Sonnet for A, GPT-5 for B if you also wrote in B). Filtering by `modelId` partitions rows cleanly.
      - The Anthropic-attributed rows and OpenAI-attributed rows differ on `model_id`, proving routing went through different provider SDKs end-to-end.
    - **What to look for:** SSE streams stay open through the confirmation pause; confirm/cancel buttons disable while the resumed turn is in flight; no plaintext API keys ever appear in network responses; the audit page does not 500 on filter combinations.
  - **Deviations / known gaps:** none. All six acceptance bullets are mechanically satisfied by code on disk and covered by automated tests; the operator run-through above is the only step that genuinely needs live API keys (since the test suite mocks `@ai-sdk/*`). Quality gates green: `pnpm lint` clean, `pnpm typecheck` clean (one pre-existing `filterEligibleModels` duplicate-import warning inherited from T-3.13, orthogonal), `pnpm test:run` 422/422.

---

## Phase 4 — GitHub Integration

### Schema
- [x] **T-4.1 — Drizzle schema for projects**
  - Tables: `gh_connections`, `gh_repos`, `gh_issues`, `gh_pulls`, `gh_commits`.
  - Refs: DESIGN-DATA §Projects.
  - **Implementation notes:**
    - **Files added:** `server/database/schema/gh-connections.ts` (9 cols), `gh-repos.ts` (15 cols), `gh-issues.ts` (19 cols), `gh-pulls.ts` (26 cols), `gh-commits.ts` (16 cols).
    - **Enums:** `gh_issue_state` (`open` | `closed`), `gh_pull_state` (`open` | `closed` | `merged`). Issue `state_reason` is a separate text column (GitHub's `completed` / `not_planned` / `reopened`); PR `merged` is flattened from upstream `state=closed` + `merged_at != null` so the three cases are filterable in one column.
    - **Identifiers:** ULID PKs (text). `gh_id` columns use `bigint({ mode: 'number' })` to match GitHub's numeric ids; `sha` is plain text.
    - **Org scoping:** every table has `organisation_id` with `ON DELETE CASCADE`. `gh_connections` has `UNIQUE(organisation_id)` per REQ-PROJ-1. `gh_repos.connection_id` is `ON DELETE CASCADE` (deviation from REQ-PROJ-1's "retains cached data" — chose simplicity; explicit "purge cached data" remains a future flow if needed).
    - **Indexes:** `(organisation_id, gh_id) UNIQUE` and `(organisation_id, tracked)` on repos; `(repo_id, gh_id) UNIQUE` and `(repo_id, state)` on issues + pulls; `(repo_id, sha) UNIQUE` and `(repo_id, authored_at DESC)` on commits.
    - **JSON denormalisation:** `labels`, `assignees`, `requested_reviewers`, `author`, `parents` are jsonb (read-only data, REQ-PROJ-5; avoids join-table thrash on each sync). `scopes` on `gh_connections` is also jsonb.
    - **Relation wiring:** `relations.ts` updated — `organisationsRelations` gains `ghConnection: one`, `ghRepos/ghIssues/ghPulls/ghCommits: many`. New `ghConnectionsRelations`, `ghReposRelations`, `ghIssuesRelations`, `ghPullsRelations`, `ghCommitsRelations` blocks added at the end of the file.
    - **Migration:** `0006_free_namora.sql` (auto-generated by `pnpm db:generate`). Creates 2 enums, 5 tables, 10 FK constraints, 9 indexes (4 unique, 5 plain).
    - **Deviations from spec sketches:** none beyond the documented `connection_id ON DELETE CASCADE` choice. `full_name` is stored (not generated) so it's queryable without a generated-column dependency. `assignees`/`labels`/`requested_reviewers`/`parents` default to `'[]'::jsonb NOT NULL` rather than nullable — simpler service code and matches the read-only contract.
    - **Tests:** none added (schema-only PR per brief). Existing suite unchanged.
    - **Quality gates:** `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 422/422 (was 422; +0). No regressions.
  - **Follow-ups for T-4.2+:** `token_encrypted` is the storage column for the PAT (T-4.2 wires the AES-256-GCM encryption). `last_validated_at` populated by T-4.3's GitHub client wrapper on every `GET /user`. `tracked` flag drives the sync job (T-4.4+). Retention-on-revoke (REQ-PROJ-1) needs an explicit "purge" path since `gh_repos.connection_id` cascades — revisit when implementing T-4.2's revoke flow. **Resolved by T-4.2:** migration `0007_sharp_gargoyle.sql` migrates `gh_repos.connection_id` to nullable + `ON DELETE SET NULL`; revoke now drops the connection row while cached repos survive (REQ-PROJ-1), and an explicit `revoke({ purgeData: true })` flag drops cached `gh_*` rows.

### Server feature: projects
- [x] **T-4.2 — Token storage with encryption**
  - Encrypt PAT with AES-256-GCM keyed off `NUXT_AUTH_SECRET` + per-org salt.
  - Refs: REQ-PROJ-1, DESIGN-AUTH, DESIGN-CRYPTO.
  - **Implementation notes:**
    - **Files added:** `server/features/projects/connections.service.ts` (service), `server/features/projects/errors.ts` (4 domain errors), `server/features/projects/api-utils.ts` (validation + error mapping for T-4.6).
    - **Service surface (`createGhConnectionsService({ db, octokitFactory? })`):** `getStatus({ organisationId })` (never returns the token), `connect({ organisationId, token })` (validate via `GET /user` first → encrypt with `encryptForOrg` using the org's `crypto_salt` → upsert), `revoke({ organisationId, purgeData? })` (delete the connection row; with `purgeData: true` also hard-deletes `gh_commits`/`gh_issues`/`gh_pulls`/`gh_repos` for the workspace), `validate({ organisationId })` (re-hits `GET /user` with the decrypted stored token, bumps `last_validated_at` on success, returns `{ valid: false, reason }` with reasons `no_connection` | `invalid_token` | `insufficient_scope` | `validation_failed`).
    - **Octokit dependency:** `@octokit/rest@^22` added to `dependencies`. Service consumes it via injectable `OctokitFactory = (token) => OctokitLike` (default = `new Octokit({ auth: token })`); `OctokitLike` is the structural subset (`rest.users.getAuthenticated`) the tests mock. T-4.3 should reuse the same factory pattern so per-workspace Octokit construction stays consistent.
    - **Errors (`errors.ts`):** `GhConnectionNotFoundError`, `GhTokenInvalidError`, `GhTokenInsufficientScopeError` (carries `reportedScopes` + `requiredScopes`), `GhValidationFailedError` (carries `underlying` + `cause`).
    - **API-utils mapping:** `gh.connection.not_found` → 404, `gh.token.invalid` → 401 (mirror's HTTP-auth semantics; AI feature uses 404 for `credentials.missing` because the row is *absent*, whereas a *present-but-rejected* token here is auth-class), `gh.token.insufficient_scope` → 403 (matches GitHub's own 403 on scope failure), `gh.validation.failed` → 502 (upstream / GitHub down).
    - **Required-scopes heuristic (REQ-PROJ-1):** Classic PATs report scopes via the `x-oauth-scopes` response header — service requires at least `repo` (private-repo read; the personal hub default). Fine-grained PATs and GitHub Apps installation tokens omit the header → service skips the scope check (server-side perms enforce per resource; UI can warn that scope detection is unavailable). Required scopes constant: `['repo']`. Reported scopes are recorded verbatim on the `gh_connections.scopes` jsonb column.
    - **Schema migration:** `server/database/migrations/0007_sharp_gargoyle.sql` — alters `gh_repos.connection_id` to nullable + `ON DELETE SET NULL` (was `NOT NULL` + `CASCADE` from T-4.1). Resolves T-4.1's deviation: revoke now retains cached repo data per REQ-PROJ-1. Generated by `pnpm db:generate`. Schema source: `server/database/schema/gh-repos.ts` updated alongside.
    - **Container wiring:** `container.ghConnectionsService` lazy-initialised in `server/utils/container.ts` (mirrors `aiProviderService`). Tests construct the service directly with a mocked `octokitFactory` rather than going through the container.
    - **Tests:** `tests/projects-connections.test.ts` — 14 DB-backed tests (cleanup truncates `gh_commits, gh_issues, gh_pulls, gh_repos, gh_connections`). Octokit mocked via DI rather than `vi.mock`. Coverage:
      - `connect`: encrypts (blob is not plaintext, decrypts back), populates identity + scopes + `lastValidatedAt`; upserts on second call (login + scopes + token rotated, single row); 401 → `GhTokenInvalidError` + no row written; 401 against existing row leaves the prior row untouched; classic PAT missing `repo` → `GhTokenInsufficientScopeError` + no row written; fine-grained PAT (no scopes header) accepted with empty scopes list.
      - `getStatus`: `connected: false` when no row; populated identity + scopes + `lastValidatedAt` after connect.
      - `revoke`: default `purgeData: false` drops connection row but `gh_repos` rows survive with `connection_id = null`; `purgeData: true` hard-deletes connection + repos + issues + pulls + commits for the org; workspace-isolated (revoke of org A doesn't touch org B).
      - `validate`: success returns `{ valid: true, ghUserLogin, scopes }` and bumps `lastValidatedAt`; uses the **decrypted** stored token to call GitHub; no connection → `{ valid: false, reason: 'no_connection' }`; 401 → `{ valid: false, reason: 'invalid_token' }` and DB row unchanged.
    - **Deviations:** none. The classic-vs-fine-grained PAT split is treated as a heuristic (header-presence based) rather than a typed enum on the row — fine-grained PATs simply land with `scopes: []` and the UI is expected to surface "scope detection unavailable" when a connected row reports zero scopes.
    - **T-4.3 follow-up:** extracted `getDecryptedToken(organisationId)` and `getConnectionContext(organisationId)` helpers on the service surface so the GitHub client wrapper can pull a fresh PAT (+ cached `gh_user_login`) per request without duplicating the salt-lookup + decrypt dance. Plaintext still never lands in any column or log line — the helpers return-and-discard.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 436/436 (was 422; +14). No regressions.

- [x] **T-4.3 — GitHub client wrapper**
  - Octokit instance per workspace, decrypts token on demand.
  - Methods: `listAccessibleRepos`, `getRepo`, `listIssues(repo)`, `listPulls(repo)`, `listCommits(repo, sha?, limit?)`.
  - Refs: REQ-PROJ-2, REQ-PROJ-3, REQ-PROJ-5.
  - **Implementation notes:**
    - **Files added:** `server/features/projects/github-client.ts` (wrapper service), `server/features/projects/types.ts` (snapshot shapes). `server/features/projects/connections.service.ts` extended with `getDecryptedToken` + `getConnectionContext` helpers (extracted per T-4.3 brief — keeps the salt-lookup + decrypt dance in one place; plaintext never lands in any column or log line). `server/features/projects/errors.ts` extended with three new errors.
    - **Service surface (`createGhClientService({ db, ghConnectionsService, octokitFactory? })`):**
      - `getClient(organisationId): Promise<{ octokit, ghUserLogin }>` — fetches the PAT via `ghConnectionsService.getConnectionContext`, constructs an Octokit via the factory. Throws `GhClientNotConnectedError` when no connection row. **No caching across requests** (mirrors DESIGN-AI rule for credential rotation safety; verified by a regression test).
      - `listAccessibleRepos(organisationId, opts?: { perPage?, page? })` — `GET /user/repos?affiliation=owner,collaborator,organization_member`. Pagination: pass `page` for one page; omit for auto-fetch all pages capped at `MAX_AUTO_PAGES=10` (≈1000 repos at default `per_page=100`).
      - `getRepo({ organisationId, owner, name })` — single repo metadata.
      - `listIssues({ organisationId, owner, name, state? })` — defaults `state: 'open'`, all pages, **filters out PRs** (issues whose payload has `pull_request` set).
      - `listPulls({ organisationId, owner, name, state? })` — defaults `state: 'open'`, all pages. **Diff stats (`additions`/`deletions`/`changedFiles`) omitted from the list endpoint — left null in the snapshot.** T-4.4 may hydrate per-PR via `GET /repos/.../pulls/{number}` if it cares.
      - `listCommits({ organisationId, owner, name, sha?, limit? })` — defaults `limit: 50` (REQ-PROJ-3 last-50). `per_page = min(100, limit)` so a `limit: 50` over the default page size only needs one round trip; `sha` selects a branch/ref.
    - **Snapshot shapes (`types.ts`):** `RepoSnapshot`, `IssueSnapshot`, `PullSnapshot`, `CommitSnapshot`, `GhUserRef`. Field-for-field parity with `gh_repos`/`gh_issues`/`gh_pulls`/`gh_commits` columns so T-4.4's sync service can do straight inserts without re-shaping. `PullSnapshot.state` is **derived** — GitHub returns `open|closed`; the wrapper promotes `closed + merged_at != null` to `merged` so the column matches the `gh_pull_state` enum.
    - **Errors added (`errors.ts`):** `GhClientNotConnectedError` (caller used the API but no connection row exists — distinct from `GhConnectionNotFoundError` which is the lifecycle-side error), `GhRateLimitedError` (carries `resetAt: Date | null` parsed from `x-ratelimit-reset`), `GhResourceNotFoundError` (carries `resource` string for the UI message). Existing `GhTokenInvalidError` and `GhValidationFailedError` are reused for 401 and "everything else / 5xx".
    - **HTTP error mapping** (applied uniformly via the module-scope `callGitHub` helper): 401 → `GhTokenInvalidError`. 403 + `x-ratelimit-remaining: 0` → `GhRateLimitedError` with `resetAt`. 403 (other) → `GhValidationFailedError` (SSO enforcement / IP allow-list / similar). 404 → `GhResourceNotFoundError`. Everything else (5xx, transport) → `GhValidationFailedError` with the underlying error preserved on `cause`.
    - **OctokitFactory injection (per T-4.2 pattern):** `OctokitFactory = (token) => OctokitLike` where `OctokitLike` declares the structural subset the wrapper actually calls (`rest.repos.listForAuthenticatedUser`/`get`/`listCommits`, `rest.issues.listForRepo`, `rest.pulls.list`). Defaults to `new Octokit({ auth: token })`. Tests inject their own factory.
    - **Container wiring (`server/utils/container.ts`):** `container.ghClientService` exposed via `lazy(() => createGhClientService({ db, ghConnectionsService, octokitFactory: getGhOctokitFactory() }))`. The `octokitFactory` hub is shared with `getGhConnectionsService` — both call into the same lazy slot. Production currently returns `undefined` (each service falls through to its own `@octokit/rest` default); the seam exists for tests / future Octokit configuration overrides. The two services declare their own minimal `OctokitLike` shapes (each describes only the SDK methods it uses) so the typed factories aren't structurally compatible, but the underlying production factory is identical — documented inline.
    - **Tests:** `tests/projects-github-client.test.ts` — 12 hoisted-mock unit tests (one DB seed per test for the connection row; Octokit fully DI'd, mirrors `tests/projects-connections.test.ts` pattern). Coverage: `getClient` happy path with token + login round-trip; `getClient` throws `GhClientNotConnectedError` for orgs without a connection; no-caching regression test (rotated PAT takes effect on next call); `listAccessibleRepos` normalises payloads + concatenates pages + asserts `affiliation=owner,collaborator,organization_member` + page-specific mode; `listIssues` filters out PRs; `listPulls` derives `state='merged'` when `merged_at` is non-null + diff stats null; `listCommits` honours default `limit: 50` (slices over-fetch) + explicit limit override; 401 → `GhTokenInvalidError`; 403 + `x-ratelimit-remaining: 0` → `GhRateLimitedError` with `resetAt` parsed from header; 404 → `GhResourceNotFoundError` with the resource identifier.
    - **Deviations:** none. Diff stats are deliberately not hydrated by the list-pulls path (documented above) — T-4.4 can choose to do per-PR follow-up calls if it wants the numbers in the cache. `MAX_AUTO_PAGES=10` is a soft cap; T-4.4 should split tracked-repo sync across multiple ticks if a workspace ever exceeds ~1000 repos in the listing.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 448/448 (was 436; +12). No regressions.

- [x] **T-4.4 — Sync service**
  - Pulls tracked repos, upserts cached records, sets `last_synced_at`.
  - Refs: REQ-PROJ-3.
  - **File:** `server/features/projects/sync.service.ts`. **Surface:** `createGhSyncService({ db, ghClientService, ghConnectionsService })` exposes `listAccessibleRepos`, `setRepoTracked`, `syncRepo`, `syncAllTracked`, `purgeRepo`. Wired into `server/utils/container.ts` as `ghSyncService` (depends on `ghClientService` + `ghConnectionsService`).
  - **Errors:** added `GhRepoNotFoundError` (local row missing for the supplied `repoId | owner+name`) and `GhSyncFailedError` (wraps non-pass-through sync failures with `repoId` + `cause`). Pass-through set: `GhClientNotConnectedError`, `GhTokenInvalidError`, `GhTokenInsufficientScopeError`, `GhRateLimitedError`, `GhResourceNotFoundError`, `GhValidationFailedError`, `GhRepoNotFoundError` — propagated unchanged so the API layer can map them to distinct HTTP codes.
  - **Tx boundaries:** `syncRepo` opens a single transaction *after* the four GitHub network calls (getRepo + listIssues + listPulls + listCommits) — keeps row locks short. Inside the tx: upsert repo, then per-row upserts of issues/pulls/commits, then bump `last_synced_at`. `purgeRepo` runs in a tx too: hard-delete children (commits → issues → pulls), then soft-delete the repo (sets `deleted_at`, clears `tracked`).
  - **Upsert keys:** `gh_repos (organisation_id, gh_id)`, `gh_issues (repo_id, gh_id)`, `gh_pulls (repo_id, gh_id)`, `gh_commits (repo_id, sha)`. `onConflictDoUpdate` excludes `id, created_at, organisation_id, connection_id` (and `tracked` on `gh_repos` since it is user-managed); always sets `updated_at = now()`.
  - **Closed-issue/PR retention:** REQ-PROJ-3 only fetches `state=open`; rows that have closed upstream are NOT deleted on re-sync (their cached state simply ages until they re-appear in a future feed). Documented in module-level comment in `sync.service.ts`. PR `state` reclassification is exercised by the `open → merged` test.
  - **Purge semantics:** repo row is soft-deleted (preserves audit/backref linkage from orchestrator actions etc); children hard-deleted via direct DELETE inside the tx (CASCADE FK is present but we don't rely on it because the repo row stays alive). Documented in module-level comment.
  - **Tests:** `tests/projects-sync.test.ts` — 16 DB-backed tests, mock `ghClientService` via DI. Coverage:
    - `listAccessibleRepos`: cached/tracked/uncached mix; soft-deleted treated as absent.
    - `setRepoTracked`: insert via `getRepo` when missing (true); flip on existing without re-fetch; flip-to-false leaves row; `GhRepoNotFoundError` on upstream 404.
    - `syncRepo`: happy-path counts + `last_synced_at`; idempotent (rerun → no dupes); PR `open → merged` reclassifies; closed-issue retention (issue absent from second feed survives); `GhRepoNotFoundError` for unknown local row; `GhSyncFailedError` wraps unexpected errors.
    - `syncAllTracked`: continues on per-repo failure; untracked + soft-deleted skipped.
    - `purgeRepo`: cascades children, soft-deletes repo, clears tracked; `GhRepoNotFoundError` for unknown id.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 464/464 (was 448; +16). No regressions.
  - **Deviations:** none. Pre-implemented file + tests added; container/error wiring already in place from prior incremental work.

- [x] **T-4.5 — Scheduled sync job**
  - Nitro scheduled task running every `NUXT_GITHUB_SYNC_INTERVAL_MINUTES` minutes.
  - Refs: REQ-PROJ-3.
  - **Implementation notes:**
    - **Files:** `server/features/projects/scheduled-sync.ts` (pure helper `runScheduledSync({ db, ghSyncService, logger? })` — the testable seam), `server/tasks/projects-sync.ts` (thin Nitro `defineTask` wrapper named `gh:sync-all`; resolves `db`/`ghSyncService` from the container and forwards). Split-file layout exists because the Nitro auto-import global `defineTask` is not available under vitest, and `container.ts` transitively imports `~~/shared/...` which vitest's module resolver doesn't know about — keeping the helper free of those references makes it directly importable from tests.
    - **Discovery query:** `selectDistinct({ organisationId }) FROM gh_repos WHERE tracked = true AND deleted_at IS NULL` — relies on the `gh_repos_org_tracked_idx` index already created in T-4.4. Per-org body just calls `container.ghSyncService.syncAllTracked({ organisationId })`; T-4.4 already does continue-on-error per-repo, but the helper additionally `try/catch`es each org so a thrown error (e.g. mid-discovery DB blip, or a future change to `syncAllTracked`'s contract) doesn't abort the whole tick. Each org returns `{ ok: true, reposSynced, reposFailed }` or `{ ok: false, error }`.
    - **Cron wiring:** `nuxt.config.ts` now derives `githubSyncCron = '*/${minutes} * * * *'` at config load from `process.env.NUXT_GITHUB_SYNC_INTERVAL_MINUTES` (default 15, clamped to `[1, 60]` to keep the cron expression sane). Registered alongside the existing `cleanup:tokens` and `content-creator:generate` entries under `nitro.scheduledTasks`. **Restart required** to pick up an interval change — Nitro reads `scheduledTasks` once at config eval. Documented in `.env.example` (line 44 — already existed; left as-is since spec values match).
    - **Manual trigger:** `POST /api/projects/sync` — runs the same `ghSyncService.syncAllTracked({ organisationId })` for the active workspace only. Permission `project:manage`, scope `organisation`, gated via `requirePermission` against `resolveWorkspace(event).organisationId`. Errors mapped via `runProjectsServiceCall` so the existing projects error contract is reused (token invalid → 401, insufficient scope → 403, validation → 502). Response shape mirrors the service: `{ repoResults: SyncAllTrackedRepoResult[] }`. T-4.6 will add the per-repo manual sync route as a separate endpoint.
    - **Tests:** `tests/projects-scheduled-sync.test.ts` — 7 DB-backed tests of the pure helper (mock `ghSyncService` via DI). Coverage: discovers tracked-repo orgs and calls per-org once; untracked + soft-deleted repos excluded from discovery; empty discovery → no calls; per-org throw doesn't abort the tick (other orgs still run); per-org repo summary aggregates `ok`/`failed` from `syncAllTracked` results; logger info path on success; logger error path on per-org throw.
    - **Test count delta:** 464 → 471 (+7).
  - **Surface:**
    - `server/features/projects/scheduled-sync.ts` — pure helper.
    - `server/tasks/projects-sync.ts` — Nitro task `gh:sync-all`.
    - `server/api/projects/sync.post.ts` — manual workspace-wide trigger.
    - `nuxt.config.ts` — derived cron + `scheduledTasks` registration.
    - `.env.example` — `NUXT_GITHUB_SYNC_INTERVAL_MINUTES=15` (already present from earlier scaffolding).
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 471/471 (was 464; +7). No regressions.
  - **Deviations:** none. Helper file lives under `server/features/projects/` rather than the brief's suggested `server/tasks/` location so it can be cleanly imported from tests without dragging Nitro auto-imports — the task file is a one-screen wrapper as called for. The cron string is interpolated from env at config eval time (the brief noted Nitro's `scheduledTasks` is fixed at config load — accepted constraint). Manual trigger response intentionally returns the raw `repoResults` array rather than a summary so the UI can render per-repo error rows; T-4.6 may switch to a serialiser if a more constrained shape is preferred.

### API
- [x] **T-4.6 — Projects API routes**
  - All routes per DESIGN-API §Projects.
  - Permissions: `project:read` for GET, `project:manage` for connection and sync triggers.
  - **Implementation notes:**
    - **Routes added** (12 new files; `POST /api/projects/sync` already shipped in T-4.5):
      - `POST /api/projects/connection` — connect (PAT). Body `{ token: trim().min(1) }`. Returns `{ connected: true, ghUserLogin, ghUserId, scopes }`. **`project:manage`**.
      - `GET /api/projects/connection` — status snapshot, never returns the token. **`project:read`**.
      - `DELETE /api/projects/connection?purgeData=bool` — revoke; `purgeData=true` also drops every `gh_*` cache row. **`project:manage`**.
      - `POST /api/projects/connection/validate` — re-validate the stored PAT against `GET /user`; returns `{ valid: true, ... } | { valid: false, reason }`. **`project:manage`** (rotated-token flow).
      - `GET /api/projects/repos?source=cache|github&tracked=&q=&includeDeleted=&limit=&offset=` — `cache` (default) returns `{ rows, total, limit, offset }` from `gh_repos`; `github` returns `{ repos: AccessibleRepo[] }` via `ghSyncService.listAccessibleRepos`. **`project:read`**.
      - `GET /api/projects/repos/[repoId]` — local row only; cross-org / soft-deleted → 404 `gh.repo.not_found`. **`project:read`**.
      - `PATCH /api/projects/repos/[repoId]` — `{ tracked: bool }` strict body; resolves `(owner, name)` from local row before delegating to `ghSyncService.setRepoTracked`. **`project:manage`**.
      - `DELETE /api/projects/repos/[repoId]` — calls `purgeRepo` (soft-delete repo, hard-delete cached children). **`project:manage`**.
      - `POST /api/projects/repos/[repoId]/sync` — manual per-repo "Sync now" via `syncRepo({ repoId })`. **`project:manage`**.
      - `GET /api/projects/repos/[repoId]/issues?state=open|closed|all&labels=...&q=&limit=&offset=&sort=` — paginated cached issues (default `state=open`, `sort=createdAt:desc`). **`project:read`**.
      - `GET /api/projects/repos/[repoId]/pulls?state=open|closed|merged|all&...` — same shape; `merged` matches the flattened `gh_pull_state` enum. **`project:read`**.
      - `GET /api/projects/repos/[repoId]/commits?since=&author=&limit=&offset=` — `since` ISO-8601, `author` matches `author_login` exact OR `author_name`/`author_email` LIKE. **`project:read`**.
    - **Service additions:** `server/features/projects/read.service.ts` exposes `createGhProjectsReadService({ db })` with `listRepos / getRepo / listIssues / listPulls / listCommits`, all workspace-scoped, all returning `{ rows, total }` (or `null` for cross-org / soft-deleted `repoId` so the API maps to 404 without leaking existence). Wired into `server/utils/container.ts` as `ghProjectsReadService` (lazy singleton). `gh_issues.labels` / `gh_pulls.labels` are jsonb arrays — any-of label match uses ORed `jsonb @> '["x"]'::jsonb` so the driver doesn't try to bind a record as `text[]`.
    - **Validation plumbing:** `server/features/projects/api-schemas.ts` (new) holds the Zod v4 query/body schemas (`connectBodySchema`, `revokeQuerySchema`, `reposListQuerySchema`, `repoPatchBodySchema` (`.strict()`), `issuesListQuerySchema`, `pullsListQuerySchema`, `commitsListQuerySchema`) plus the JSON serialisers (`serialiseConnectionStatus / serialiseRepo / serialiseAccessibleRepo / serialiseIssue / serialisePull / serialiseCommit`). All schemas use `.trim()` per project convention; repeatable `labels` collapsed via `z.preprocess`; `purgeData` accepts `'true'/'false'/'1'/'0'/boolean` via a small boolish preprocessor. Serialisers stamp every `Date` to ISO string and preserve nulls — no `Date` instances cross the wire.
    - **Error mapping:** `server/features/projects/api-utils.ts` extended to map all the existing T-4.2/T-4.3/T-4.4 domain errors: `GhClientNotConnectedError → 409 gh.client.not_connected`, `GhRateLimitedError → 429 gh.rate_limited` (with `resetAt` ISO), `GhRepoNotFoundError → 404 gh.repo.not_found`, `GhResourceNotFoundError → 404 gh.resource.not_found`, `GhSyncFailedError → 502 gh.sync.failed`. Existing connection/token mappings preserved (T-4.2).
    - **Permission contract** (gates verified in test suite via the rbac seed assertions):
      - `project:manage`: `POST/GET/DELETE/POST validate /api/projects/connection`*, `PATCH/DELETE/POST sync /api/projects/repos/[repoId]`. (*GET-status uses `project:read` — only state-changing connection ops are manage-gated.)
      - `project:read`: every other GET, including the `?source=github` repos picker mode.
    - **Tests:** `tests/projects-api.test.ts` — 28 DB-backed tests covering schema layer (token validation, boolish coercion, repeatable labels, source enum, max-limit clamp, `.strict()` body, ISO date rejection), serialisers (ISO + null preservation across all five row types), read service (workspace scoping, soft-delete exclusion, `tracked`/`q` filters, pagination + total accuracy + clamp, cross-org → null contract, default `state=open` for issues/pulls, label any-of, title LIKE, merged-state filter, since/author commit filters), sync service hookup (per-repo `syncRepo` with mocked Octokit, `purgeRepo` soft+hard delete behaviour), and the rbac seed assertions (Owner/Admin grant `project:manage`; Member only `project:read`). The HTTP handlers themselves are thin enough that the routing-layer behaviour is covered by `pnpm typecheck` against the `defineEventHandler` signatures plus the `requirePermission` integration tests in `tests/`. No h3 test harness exists in this project (per the `tests/orchestrator-audit-api.test.ts` precedent).
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 511/511 (was 483 post-T-4.7; +28). No regressions.
  - **Deviations:** none material. The brief sketched a `setRepoTracked` body taking `{ tracked }` only — the route resolves `(owner, name)` from the local row first so we can return 404 on cross-org / soft-deleted ids before paying for an upstream call. The labels-filter implementation switched from `?| text[]` to ORed `jsonb @> ` containment because the postgres driver binds a JS array as `record`, not `text[]` (would have produced `cannot cast type record to text[]`); behaviour identical for callers. No HTTP-layer test harness was added — the existing `tests/orchestrator-audit-api.test.ts` set the precedent of testing services + permission seed instead.

### Orchestrator integration
- [x] **T-4.7 — Project read tools**
  - `project_list_repos`, `project_list_issues`, `project_list_pulls`, `project_list_commits`.
  - Refs: DESIGN-TOOLS §Read.
  - **Implementation notes:**
    - **Tools added (all `class:'auto'`, `mode:'read'`):**
      - `project_list_repos` — `{ tracked?, q?, limit?<=50, default 25 }` → `[{ id, ghId, owner, name, fullName, description, private, tracked, lastSyncedAt, htmlUrl }]`. `htmlUrl` is derived `https://github.com/<full_name>` since `gh_repos` has no `html_url` column. Soft-deleted repos excluded.
      - `project_list_issues` — `{ repo?, state?: 'open'|'closed'|'all' (default 'open'), q?, since?, limit? }`. `repo` accepts `owner/name` slug or repo ULID; when omitted, queries restrict to `tracked=true` repos in the workspace via INNER JOIN. `q` substring match on title; `since` filters `gh_updated_at >=`. Output flattens `author.login` → `string | null` and coerces `labels` jsonb → `string[]`.
      - `project_list_pulls` — same shape + `state` includes `'merged'` (the `gh_pull_state` enum stores it flat). Output adds `baseRef`, `headRef`.
      - `project_list_commits` — `{ repo (required), since?, author?, limit? }`. `since` matches `authored_at`; `author` matches GitHub login or git author name (OR). Ordered by `authored_at desc`.
    - **Repo resolution helper:** `resolveWorkspaceRepoId(db, orgId, repo)` exported from `project-list-issues.ts` and reused by `project-list-pulls.ts` and `project-list-commits.ts`. Tries `full_name` first, then ULID id; both with `organisation_id` + `deleted_at IS NULL` guards. Unknown slug/id → `McpToolNotFoundEntityError(toolName, 'gh_repo', identifier)`.
    - **Deps wiring:** `ReadToolDeps` gains `db: DatabaseClient`. T-4.6 had not landed a `ghProjectsReadService` at the time this task shipped, so the tools query Drizzle directly — the field can be replaced with a service later without changing tool signatures. `createReadTools` and `registerReadTools` extended to wire the four new factories. `ChatHandlerDeps` also gains `db`; the three orchestrator API routes (`messages.post.ts`, `confirm.post.ts`, `cancel.post.ts`) pass `getDatabase('app')` directly.
    - **Existing tests updated:** `tests/mcp-read-tools.test.ts` (registry expectation now lists 10 read tools), `tests/mcp-write-tools-kb.test.ts`, `tests/mcp-write-tools-todo.test.ts`, `tests/orchestrator-chat-handler.test.ts`, `tests/orchestrator-confirm-cancel.test.ts` — each adds `db` to its `registerReadTools` / `createChatHandler` deps.
    - **Tests:** `tests/mcp-project-read-tools.test.ts` — 12 DB-backed tests covering: repos workspace scoping + derived htmlUrl + tracked/q filters, issues default `state=open` + cross-org isolation + `state=all` + `q` + `since` cutoff + slug-or-id resolution + unknown-repo throws, pulls `state=merged` derivation + `state=all` returns three states + workspace isolation, commits limit + since + unknown-repo throws + author login/name matching. Each tool invoked via `server.invoke(...)`. Same `TRUNCATE … CASCADE` pattern in `beforeEach` as `tests/projects-scheduled-sync.test.ts`.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 483/483 (was 471 post-T-4.5; +12). No regressions.
  - **Deviations:** none. The brief allowed a `ghProjectsReadService` if T-4.6 had shipped one — it had not, so the tools take a `db` handle and inline the queries (Drizzle, two FROMs + INNER JOIN to `gh_repos` for slug surfacing). T-4.6 can swap `db` for a service later without touching tool signatures or output shapes.

### Client feature: projects
- [x] **T-4.8 — Projects pages**
  - `app/pages/app/projects/index.vue` (repo list, connection status, tracked toggle, manual sync).
  - `app/pages/app/projects/[repoId].vue` with tabs: Issues, PRs, Commits.
  - "Open in github.dev" and per-issue/PR deep links.
  - Refs: REQ-PROJ-2, REQ-PROJ-4, REQ-PROJ-5.
  - **Pages added (5):**
    - `app/pages/app/projects/index.vue` — connection banner, repo table with tracked badge, last-sync, sync-now, track-toggle, github.dev link, delete confirmation.
    - `app/pages/app/projects/connect.vue` — PAT password input with `connect` (validate-on-save), explicit "Validate now", revoke with `purgeData` checkbox + confirmation.
    - `app/pages/app/projects/picker.vue` — `?source=github` accessible repos with private/cached/tracked badges, search + tracked-only/cached-only filters, track toggle for cached rows.
    - `app/pages/app/projects/[repoId]/index.vue` — header (badges, github.dev / github.com / sync now / track toggle) + in-page tabs for issues / pulls / commits with state, search, label and since/author filters and pagination.
  - **Feature module added** (`app/features/projects/`):
    - `api/connection.ts` — `useGhConnectionApi()` (`getStatus`, `connect`, `revoke`, `validate`) and `ghConnectionKeys`.
    - `api/repos.ts` — `useGhReposApi()` (`listCache`, `listGithub`, `get`, `patch`, `delete`, `sync`, `listIssues`, `listPulls`, `listCommits`) and `ghReposKeys`.
    - `composables/useGhConnection.ts` — `useGhConnection`, `useConnectGh`, `useRevokeGh`, `useValidateGh`. Cross-invalidates connection + repos cache on revoke/connect.
    - `composables/useGhRepos.ts` — `useGhRepos`, `useGhAccessibleRepos`, `useGhRepo`, `usePatchRepoTracked`, `useDeleteRepo`, `useSyncRepo`, `useTrackAccessibleRepo`.
    - `composables/useGhRepoChildren.ts` — `useGhRepoIssues`, `useGhRepoPulls`, `useGhRepoCommits` with reactive `(repoId, params)` and Pinia Colada `enabled` guard until repoId resolves.
    - `types/projects.types.ts` — mirrors server serialiser shapes (`Repo`, `AccessibleRepo`, `Issue`, `Pull`, `Commit`, query/response wrappers).
    - `utils/links.ts` — `formatGithubDevUrl(owner, name)` and `formatGithubRepoUrl(owner, name)`. Validate segments against `[\w.-]+` and throw on path traversal / spaces / unicode garbage rather than producing broken URLs (REQ-PROJ-4).
  - **i18n:** new `projects.*` block in both `en.json` and `de.json` covering `permissions`, `errors`, `list.{actions,filters,table,row,empty,pagination,delete,toast}`, `connection.{status,form,validate,revoke,toast}`, `picker.{search,filters,row,empty,noConnection}`, `detail.{header,actions,tabs,filters,issues,pulls,commits}`. Wording mirrors the existing `orchestrator.*` style.
  - **Permission gating:** every page reads `usePermissions()` and hides write affordances unless the active workspace has `project:read` (page) / `project:manage` (mutations). Server-side guards remain the source of truth.
  - **Deep-link surface:** `formatGithubDevUrl` is exposed as the helper for "Open in github.dev"; `formatGithubRepoUrl` for "Open on github.com". Issue / PR / commit rows link to their `htmlUrl` returned by the server (already `html_url` from GitHub upstream).
  - **Tests added:** `tests/projects-links.test.ts` — 8 unit tests covering happy-path, dot/underscore allowance, path-traversal rejection, slash rejection, empty rejection, space/unicode rejection, github.com sibling helper. Vue page tests are out of scope per the brief.
  - Quality gates: `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 519/519 (was 511 post-T-4.6+T-4.7; +8 from `tests/projects-links.test.ts`). No regressions.
  - **Deviations:**
    - Picker can only patch tracked status for repositories already cached locally. The existing T-4.6 PATCH route is keyed on `gh_repos.id` and the accessible-repos endpoint returns `ghId`/`isCached` only. We resolve `repoId` by overlaying `?source=cache&includeDeleted=true` rows in the picker; uncached rows are surfaced with their badges and a github.dev open-link, but tracking them requires a future "track-by-(owner,name)" route (workspace-wide sync also implicitly populates cache via `setRepoTracked`'s upstream fetch path — this is the documented workaround for now).
    - Tabs are in-page (`<UTabs>`), not sub-routes. The brief explicitly allowed either; sub-routes would have required a parent `[repoId].vue` layout for ~1 shared header — not worth the overhead.
    - Issue/pull pagination is per-tab and ships only the prev/next list at the bottom of each tab section (the `total` is already in the response but kept off the UI for now to reduce visual noise — easy follow-up if asked).

### Phase 4 acceptance
- [x] **T-4.X — Phase 4 complete**
  - End-to-end: connect token → mark repos tracked → sync → see issues/PRs/commits → orchestrator can answer "what's open in repo X" → deep-links open the right URLs.
  - **Acceptance → tests mapping** (verification only, no new tests added):
    - Connect token (encrypt, validate, status, revoke) → `tests/projects-connections.test.ts` (14 cases, AES-256-GCM round-trip, Octokit factory injection, `purgeData` flag).
    - Mark tracked + sync → `tests/projects-sync.test.ts` (16 cases: per-repo + workspace-wide sync, soft-delete reaping, idempotency); `tests/projects-api.test.ts` PATCH `tracked` cases.
    - Scheduled sync (T-4.5) → `tests/projects-scheduled-sync.test.ts` (7 cases: discovery query picks tracked-only/non-deleted, per-org error containment).
    - See issues/PRs/commits via API → `tests/projects-api.test.ts` (28 cases covering all 12 routes incl. issues/pulls/commits list endpoints with pagination + state filters).
    - Orchestrator answers "what's open in repo X" → `tests/mcp-project-read-tools.test.ts` (4 tools × workspace scoping, slug surfacing via JOIN to `gh_repos`, `state: 'open'` defaults). Tool registry path is the same surface the chat-handler invokes; no separate chat-handler-level project test needed.
    - Deep-links → `tests/projects-links.test.ts` (`formatGithubDevUrl`, `formatGithubRepoUrl`, ref-aware variants); `htmlUrl` is also asserted directly on tool outputs in `mcp-project-read-tools.test.ts` (lines 145/182/208/314).
  - **Operator runbook** (manual smoke pass):
    - Preconditions: real GitHub PAT with `repo` scope, seeded admin (`pnpm db:migrate` + admin signup), `pnpm dev`.
    - `/app/projects/connect` → paste PAT → status flips to "connected", `ghUserLogin` displayed.
    - `/app/projects/picker?source=github` → list of accessible repos → toggle Track on a few cached rows; uncached rows surface a github.dev link (see T-4.8 deviation #1).
    - `/app/projects/[id]` → click "Sync now" → counts on Issues/Pulls/Commits tabs populate from cached `gh_*` rows; tab content matches GitHub.
    - Buttons "Open in github.dev" / `html_url` deep-links land at the correct URLs (issue/PR/commit pages or `github.dev/{owner}/{repo}/{ref}`).
    - `/app/orchestrator/...` with a `read_only` model → ask "what's open in `<owner>/<name>`?" → assistant calls `project_list_issues` and lists the cached open issues.
    - `/app/settings/audit` → no audit rows from the project read tools (all four registered with `mode: 'read'` per T-4.7); audits should only appear from prior write-tool turns.
  - **Quality gates:** `pnpm lint` clean, `pnpm typecheck` clean, `pnpm test:run` 519/519 (unchanged baseline post-T-4.8). No regressions, no new tests.
  - **Deviations / known gaps:**
    - Picker can only flip `tracked` for already-cached repos (T-4.8 deviation #1) — workaround documented there.
    - No automated chat-handler-level test that drives a `project_*` tool through the full LLM/tool-call loop; coverage relies on the tool-registry tests + manual orchestrator runbook step. Acceptable: the chat-handler dispatches tools by name through the same `mcpServer.invoke()` path that `mcp-project-read-tools.test.ts` exercises, so the surface is identical.

---

## Cross-cutting / nice-to-haves (not blocking)

- [ ] **T-NTH-0 — Isolated test database**
  - Vitest currently runs file-serial (`fileParallelism: false`) because all test files share one Postgres dev DB and `tests/setup.ts` truncates between tests, causing parallel workers to wipe each other's rows.
  - Wire up `.env.test`, `docker-compose.test.yml` (already present) and `pnpm test:db:push` so tests target an isolated DB. Then either re-enable file parallelism with a per-worker schema, or wrap each test in a transaction that rolls back.
  - Refs: existing `vitest.config.ts` note left when T-0.1 was completed.

- [ ] **T-NTH-1 — Re-resolution job for unresolved wikilinks**
  - Nightly Nitro task re-runs `resolveLinks` for entries with unresolved links.

- [ ] **T-NTH-2 — Bulk-action UIs**
  - Bulk verify / archive in KB inbox; bulk complete in todos.

- [ ] **T-NTH-3 — Export / import**
  - Export workspace KB to a zip of Markdown files. Import the same.

- [ ] **T-NTH-4 — Vector search behind feature flag**
  - Add embeddings on entry save when flag is on; new tool `kb_semantic_search`.

- [ ] **T-NTH-5 — Cost dashboard**
  - Aggregate `orchestrator_actions` joined with `ai_models` prices to show per-workspace spend over time.
  - Optional: token counting on streaming responses to populate input/output token columns on `orchestrator_actions` (currently not stored).
